## 근거노선 count=16
--- 근거노선 #1 @262 ---
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<title>청원구 환경위생과 | 청원구 쓰레기 수거일 안내 v27</title>
<!-- v67: 내수읍·북이면은 원본 차량×요일 수거코스의 항목 순서를 따라 도로망 차량이동경로를 추정해 ② 근거노선에 포함하고, 주소는 표시 수거노선 30m 이내에서만 일정을 확정합니다. 오창·내수·북이는 리 단위로 시각 그룹을 유지하되 내수·북이 전체 리를 수거대상으로 보지 않습니다. 오창산업단지는 제일환경 대행구간으로 별도 표시하며, 대행업체 공동주택(아파트) 노선·주소일정은 전부 제외합니다. -->
<style>
:root{
  --bg:#f6f7f9;--panel:#fff;--text:#111827;--muted:#6b7280;--line:#e5e7eb;
  --general:#17745a;--food:#df6b42;--recycle:#315ea8;--warn:#9a6700;
}
*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;font-family:Pretendard,"Noto Sans KR",-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--text);background:var(--bg)}button,input{font:inherit}
#landing{
  position:fixed;inset:0;z-index:30;display:flex;align-items:center;justify-content:flex-start;
  padding:clamp(24px,5vw,76px);transition:.32s ease;overflow:hidden;
  background:url("data:image/jpeg;base64,
--- 근거노선 #2 @473259 ---
  </div>
    <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="rout
--- 근거노선 #3 @474468 ---
>음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>
      <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>
      <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>
      <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>
      <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel">취소
--- 근거노선 #4 @576090 ---
ict='',currentLegalEmd='',currentLegalRi='',currentAddress='',currentLat=null,currentLng=null,currentFoodExclusion=null,activeLayer='general',activeDay='전체';
const selectedVehicle={general:'전체',recycle:'전체'};
let zoneTooltipOverlay=null;
const zonePolygons={general:[],food:[],foodExclusion:[],recycle:[],districtOutline:[]};

// 직영 동지역 + 대행업체 원본 수거노선을 차량·요일별 참고 노선으로 표시합니다.
const DONG_ROUTE_DISTRICTS=new Set(['우암동','내덕1동','내덕2동','오근장동','율량사천동']);
// v67: 내수·북이는 표시된 수거노선에서 30m 이내만 자동 수거대상으로 인정합니다.
const RURAL_INFERRED_ROUTE_DISTRICTS=new Set(['내수읍','북이면']);
const RURAL_ROUTE_MATCH_KM=0.030;
// ② 근거노선: 직영은 동지역 중심으로 유지하되, 대행업체가 제공한 청원구 원본 노선은 읍·동 구분 없이 실제 대행구간을 표시합니다.
const VISUAL_DONG_ROUTE_DISTRICTS=new Set(['우암동','내덕1동','내덕2동','오근장동','율량사천동']);
const CHEONGWON_ROUTE_DISTRICTS=new Set(['내수읍','오창읍','북이면','우암동','내덕1동','내덕2동','오근장동','율량사천동']);
const districtFeatureMap=new Map();
const riFeatureMap=new Map(); // 읍·면 주소의 리 내부 세부 코스 판정용
const dongRouteOverlays={general:[],recycle:[]};
const dongRouteBuiltKeys=new Set();
const routePlaceSearchCache=new Map();

// 동지역 수거노선 영구 캐시.
// 첫 방문에는 백그라운드에서 차량×요일 노선을 계산하고 IndexedDB에 저장합니다.
// 이후 같은 브라우저에서는 저장된 도로망 좌표를 즉시 사용하므로 차량/요일 전환이 빠릅니다.
const DONG_ROUTE_CACHE_DB='cjwaste-dong-route-cache';
const DONG_ROUTE_CACHE_STORE='routes';
const DONG_ROUTE_CACHE_VERSION='v75-naedeok2-apartment-only';
const dongRouteBuildPromises=new Map();
let dongRoutePreloadStarted=false;

function routeTextHash(s){
  let h=2166136261>>>0;
  for(const ch of String(s||'')){
    h^=ch.charCodeAt(0);
    h=Math.imul(h,16777619)>>>0;
  }
  return h.toString(36);
}
function routeCacheKey(spec){
  return `${DONG_ROUTE_CACHE_VERSION}|${spec.type}|${spec.vehicle}|${spec.day}|${routeTextHash(spec.raw)}`;
}
function openDongRouteCache(){
  return new Promise((resolve,rej
--- 근거노선 #5 @587519 ---
"금":[]},"6244":{"월":[],"화":[],"수":[],"목":[],"금":[]},"6224":{"월":["팔결로117","팔결로97"],"화":[],"수":[],"목":[],"금":["팔결로321"]},"6157":{"월":[],"화":[],"수":[],"목":[],"금":[]}},"recycle":{"0263":{"월":[],"화":[],"수":[],"목":[],"금":[]},"0261":{"월":[],"화":[],"수":[],"목":[],"금":[]},"9090":{"월":[],"화":[],"수":[],"목":[],"금":[]},"0264":{"월":["율봉로95","주성로89","사뜸로49","공항로229"],"화":["토성로48","내수로199","충청대로233"],"수":["발산로6-9"],"목":["율봉로95","주성로89","충청대로233","2순환로278-1","사뜸로49"],"금":["팔결로188"]}}};

// 읍·면은 법정리 면레이어로 처리하고, 동지역 노선선은 차량 전체 코스에서 실제 등장하는 동 권역을 허용합니다.
// 일자별 수동 추론 누락 때문에 도로가 사라지던 문제를 막기 위한 차량 단위 범위입니다.
// ② 직영 근거노선은 동지역 중심으로 유지하고, 새로 제공된 대행업체 노선은 청원구 대행구간에 한해 추가 표시합니다.
const ROUTE_DONG_SCOPE={
  general:{
    '3346':['율량사천동','오근장동'],
    '6244':['율량사천동','오근장동'],
    '6224':['오근장동','율량사천동'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '9090':['율량사천동','오근장동'],
    '0264':['오근장동','율량사천동']
  }
};
// 읍·면 ① 수거권역은 기존 원자료 기반 권역판정을 유지하기 위한 별도 범위입니다.
// 이 범위는 ② 근거노선 레이어/차량노선 선택에는 사용하지 않습니다.
const SERVICE_ZONE_ROUTE_SCOPE={
  general:{
    '3344':['오창읍','북이면'],
    '3346':['율량사천동','오근장동','내수읍'],
    '6244':['율량사천동','오근장동'],
    '6224':['내수읍','북이면','오근장동','율량사천동'],
    '6138':['내수읍','북이면'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '6544':['내수읍','북이면'],
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '0262':['오창읍','북이면'],
    '9090':['율량사천동','오근장동','내수읍'],
    '0264':['내수읍','북이면','오근장동','율량사천동']
  }
};
// 원본 코스표를 차량×요일 단위로 다시 감사한 동지역 범위입니다.
// 차량 단위 범위보다 우선 적용하여, 예를 들어 6244/0263 노선이 오근장동으로 번지는 것을 막습니다.
const ROUTE_DONG_DAY_SCOPE={
  general:{
    '6244':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']}
  },
  recycle:{
    '0263':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '9090':{월
--- 근거노선 #6 @587930 ---
로95","주성로89","충청대로233","2순환로278-1","사뜸로49"],"금":["팔결로188"]}}};

// 읍·면은 법정리 면레이어로 처리하고, 동지역 노선선은 차량 전체 코스에서 실제 등장하는 동 권역을 허용합니다.
// 일자별 수동 추론 누락 때문에 도로가 사라지던 문제를 막기 위한 차량 단위 범위입니다.
// ② 직영 근거노선은 동지역 중심으로 유지하고, 새로 제공된 대행업체 노선은 청원구 대행구간에 한해 추가 표시합니다.
const ROUTE_DONG_SCOPE={
  general:{
    '3346':['율량사천동','오근장동'],
    '6244':['율량사천동','오근장동'],
    '6224':['오근장동','율량사천동'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '9090':['율량사천동','오근장동'],
    '0264':['오근장동','율량사천동']
  }
};
// 읍·면 ① 수거권역은 기존 원자료 기반 권역판정을 유지하기 위한 별도 범위입니다.
// 이 범위는 ② 근거노선 레이어/차량노선 선택에는 사용하지 않습니다.
const SERVICE_ZONE_ROUTE_SCOPE={
  general:{
    '3344':['오창읍','북이면'],
    '3346':['율량사천동','오근장동','내수읍'],
    '6244':['율량사천동','오근장동'],
    '6224':['내수읍','북이면','오근장동','율량사천동'],
    '6138':['내수읍','북이면'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '6544':['내수읍','북이면'],
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '0262':['오창읍','북이면'],
    '9090':['율량사천동','오근장동','내수읍'],
    '0264':['내수읍','북이면','오근장동','율량사천동']
  }
};
// 원본 코스표를 차량×요일 단위로 다시 감사한 동지역 범위입니다.
// 차량 단위 범위보다 우선 적용하여, 예를 들어 6244/0263 노선이 오근장동으로 번지는 것을 막습니다.
const ROUTE_DONG_DAY_SCOPE={
  general:{
    '6244':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']}
  },
  recycle:{
    '0263':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '9090':{월:['율량사천동'],화:['율량사천동','오근장동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '0264':{월:['율량사천동','오근장동'],화:['율량사천동','오근장동'],수:['율량사천동','오근장동'],목:['율량사천동','오근장동'],금:['오근장동']}
  }
};
function routeDongScope(type,vehicle,day=''){
  const cm=contractRouteMeta(vehicle);if(cm)return [canonicalDistrict(cm.district)];
  const exact=day&&ROUTE_DONG_DAY_SCOPE[type]?.[vehicle]?.[day];
  return [...(exact||ROUTE_DONG_SCOPE[ty
--- 근거노선 #7 @621443 ---
span style="color:#6b7280">※ 주소가 표시 수거노선 30m 이내로 매칭되면 해당 노선의 더 세부적인 결과를 우선 적용합니다.</span>${apartment}</div>`;
  return true;
}
function renderNoNearbyRouteSchedule(type,district){
  selectedVehicle[type]='전체';
  document.getElementById(type+'Badge').textContent='권역 미확정';
  renderDays(type+'Days',[]);
  const apartmentNotice=type==='recycle'
    ? '<br><br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 수거에 대해 자체 위탁계약을 체결하여 처리합니다.'
    : '';
  document.getElementById(type+'Detail').innerHTML=
    `<div class="pending"><strong>${escapeHtml(district)} 주소 위치 기준</strong><br>`+
    `현재 자동 수거권역을 확정할 수 있는 근거노선이 부족해 이 주소의 ${type==='general'?'생활':'재활용'} 수거요일을 자동 지정하지 않습니다. 관리자 수거권역을 확인하거나 원본 코스를 추가 확인해주세요.${apartmentNotice}</div>`;
}
async function nearestRouteMatchesForAddress(type,ctx){
  const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');
  if(!DONG_ROUTE_DISTRICTS.has(district)&&!RURAL_INFERRED_ROUTE_DISTRICTS.has(district))return null;
  if(!Number.isFinite(+ctx.lat)||!Number.isFinite(+ctx.lng))return null;

  const specs=routeSpecsForDistrict(type,district);
  if(!specs.length)return [];

  const items=await ensureDistrictRouteItems(type,district);
  const itemMap=new Map(items.map(item=>[`${item.vehicle}|${item.day}`,item]));
  const addressRoads=extractAddressRoadNames(ctx.address);

  const maxRouteKm=routeMatchLimitKm(district);
  const measures=specs.map(spec=>{
    const item=itemMap.get(`${spec.vehicle}|${spec.day}`)||null;
    const distanceKm=item?routeItemDistanceKm(item,+ctx.lat,+ctx.lng):Infinity;
    const roads=matchedAddressRoads(spec,addressRoads);
    return {spec,item,distanceKm,roads,roadHit:roads.length>0};
  });

  // 주소점에서 실제 표시 수거선까지 동지역 20m, 내수·북이 30m 이내인 노선만 후보로 인정합니다.
  const eligible=measures.filter(m=>Number.isFinite(m.distanceKm)&&m.d
--- 근거노선 #8 @664255 ---
n {...data,segments,servicePoints:[],zoneEvidencePoints};
}
function existingDongRouteOverlay(spec){
  return (dongRouteOverlays[spec.type]||[]).find(x=>x.vehicle===spec.vehicle&&x.day===spec.day) || null;
}
function createDongRouteOverlay(spec,data){
  let segments=Array.isArray(data.segments)?data.segments:[];
  segments=segments
    .map(seg=>(seg||[]).filter(p=>Number.isFinite(+p.lat)&&Number.isFinite(+p.lng)))
    .filter(seg=>seg.length>=2);

  const servicePoints=[];
  const zoneEvidencePoints=uniqueRoutePoints(Array.isArray(data.zoneEvidencePoints)?data.zoneEvidencePoints:[]);

  // ② 근거노선에 그릴 선이 없어도, ① 수거권역을 나누는 시설/마을 근거점이 있으면 데이터 항목은 유지합니다.
  if(!segments.length&&!zoneEvidencePoints.length)return null;

  const termsCount=Number(data.termsCount)||routeDescriptorsForMap(spec.raw).length;
  const pointsCount=Number(data.pointsCount)||0;
  const serviceTermCount=Number(data.serviceTermCount)||0;
  const missingTerms=Array.isArray(data.missingTerms)?data.missingTerms:[];
  const outsideTerms=Array.isArray(data.outsideTerms)?data.outsideTerms:[];
  const outOfCheongwonTerms=Array.isArray(data.outOfCheongwonTerms)?data.outOfCheongwonTerms:[];
  const rangeTerms=Array.isArray(data.rangeTerms)?data.rangeTerms:[];
  const requiredRoadTerms=Array.isArray(data.requiredRoadTerms)?data.requiredRoadTerms:[];
  const coveredRoadTerms=Array.isArray(data.coveredRoadTerms)?data.coveredRoadTerms:[];
  const missingRoadTerms=Array.isArray(data.missingRoadTerms)?data.missingRoadTerms:[];
  const outsideRoadTerms=Array.isArray(data.outsideRoadTerms)?data.outsideRoadTerms:[];
  const addressRangeTerms=Array.isArray(data.addressRangeTerms)?data.addressRangeTerms:[];
  const secondaryText=String(data.secondaryText||'').trim();
  const secondaryKind=String(data.secondaryKind|| (spec.ty
## 추정권역 count=14
--- 추정권역 #1 @473239 ---

      </article>
    </div>
    <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button 
--- 추정권역 #2 @474411 ---
button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>
      <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>
      <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>
      <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>
      <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id
--- 추정권역 #3 @474763 ---
n>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>
      <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>
      <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>
      <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>
      <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel">취소</button><button id="adminRebuild">자동권역 재표시</button></div>
      <div id="adminStatus" class="admin-status">저장된 수동 권역을 불러오는 중입니다.</div>
      <button id="adminExport" class="wide-btn">수동권역 JSON 내보내기</button>
      <button id="adminImport" class="wide-btn">수동권역 JSON 가져오기</button><input id="admin
--- 추정권역 #4 @474809 ---
 <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>
      <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>
      <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>
      <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>
      <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel">취소</button><button id="adminRebuild">자동권역 재표시</button></div>
      <div id="adminStatus" class="admin-status">저장된 수동 권역을 불러오는 중입니다.</div>
      <button id="adminExport" class="wide-btn">수동권역 JSON 내보내기</button>
      <button id="adminImport" class="wide-btn">수동권역 JSON 가져오기</button><input id="adminImportFile" type="file" accept="application/js
--- 추정권역 #5 @633254 ---
act){renderContractExactSchedule(type,exact,ctx);continue}
    if(isUnconfirmedRi(type,district,ctx.legalRi||''))continue;
    if(RURAL_INFERRED_ROUTE_DISTRICTS.has(district)){
      const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});
      if(seq!==nearbyScheduleRequestSeq)return;
      if(near?.length)renderNearbyRouteSchedule(type,near,ctx);
      else if(!renderRestoredRuralRiSchedule(type,ctx))renderNoNearbyRouteSchedule(type,district);
      continue;
    }
    if(DONG_ROUTE_DISTRICTS.has(district)){
      // 확실한 표시 수거노선 20m 이내가 최우선입니다. 그 밖의 빈 공간에서만 추정권역을 사용합니다.
      const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});
      if(seq!==nearbyScheduleRequestSeq)return;
      if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}
      const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});
      if(seq!==nearbyScheduleRequestSeq)return;
      if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);
      else renderNoNearbyRouteSchedule(type,district);
      continue;
    }
    // 오창읍은 기존 리 단위/대행 확정규칙을 유지합니다.
    const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});
    if(seq!==nearbyScheduleRequestSeq)return;
    if(match)renderServiceZoneSchedule(type,match,ctx);
    else renderNoNearbyRouteSchedule(type,district);
  }
  if(seq!==nearbyScheduleRequestSeq)return;
  renderVehicleFilter();
  if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer);
}

function renderVehicleFilter(){
  const el=document.getElementById('vehicleFilter');
  const serviceLayer=activeLayer==='general'||activeLayer==='recycle';
  el.classList.toggle('show',serviceLayer);
  if(!serviceLayer){e
--- 추정권역 #6 @688349 ---
ings.length?meta.hitRings:normalizedDisplayRings).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng})));
  const zone={polygon,type,...meta,points:[...(exactRings[0]||[])],rings:normalizedDisplayRings,hitRings:exactRings};
  kakao.maps.event.addListener(polygon,'mouseover',e=>{
    polygon.setOptions({fillOpacity:meta.manual?.38:(meta.ambiguous?.26:(contract?.48:(rural?.40:.34)))});
    const ratioText=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';
    const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);
    const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));
    showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);
  });
  kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));
  kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});
  kakao.maps.event.addListener(polygon,'click',()=>{
    if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 30m 이내에서만 판정`);return}
    updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);
    if(activeLayer===type){
   
--- 추정권역 #7 @688532 ---
ayRings,hitRings:exactRings};
  kakao.maps.event.addListener(polygon,'mouseover',e=>{
    polygon.setOptions({fillOpacity:meta.manual?.38:(meta.ambiguous?.26:(contract?.48:(rural?.40:.34)))});
    const ratioText=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';
    const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);
    const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));
    showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);
  });
  kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));
  kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});
  kakao.maps.event.addListener(polygon,'click',()=>{
    if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 30m 이내에서만 판정`);return}
    updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);
    if(activeLayer===type){
      renderServiceZoneSchedule(type,{vehicle:zone.vehicle,vehicles:zone.vehicles,provider:zone.provider,plate:zone.plate,days:zone.days,riGrouped:!!zone.riGrouped,contractLayer:!!zone.c
--- 추정권역 #8 @689444 ---

  });
  kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));
  kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});
  kakao.maps.event.addListener(polygon,'click',()=>{
    if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 30m 이내에서만 판정`);return}
    updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);
    if(activeLayer===type){
      renderServiceZoneSchedule(type,{vehicle:zone.vehicle,vehicles:zone.vehicles,provider:zone.provider,plate:zone.plate,days:zone.days,riGrouped:!!zone.riGrouped,contractLayer:!!zone.contractLayer,zoneStatus:meta.manual?'manual':(meta.estimated?'estimated':(meta.ambiguous?'boundary':'inferred')),estimated:!!meta.estimated,neighborRatio:Number.isFinite(+meta.neighborRatio)?+meta.neighborRatio:0,neighborSupport:+meta.neighborSupport||0,neighborTotal:+meta.neighborTotal||0,distanceKm:Infinity},{district:meta.district||'',legalEmd:meta.district||'',legalRi:meta.riName||''},{fromMap:true});
    }
  });
  serviceZoneOverlays[type].push(zone);return zone;
}
function featureOuterPathPoints(feature){
  const rings=outerRingsOfFeature(feature);return rings.flatMap(r=>r.map(([lng,lat])=>({lat:+lat,lng:+lng})));
}
function wholeFeatureZone(type,feature,meta){
  const g=feature?.geometry;if(!g)return;
  meta={...meta,rural:meta.rural??RI_LAYER_DISTRICTS.has(meta.district||'')};
  const polys=g.type==='Polygon'?[g.coordinates]:(g.type==='MultiPolygon'?g.coordinates:[]);
  polys.forEach(poly=>{
## 수거권역 count=39
--- 수거권역 #1 @472956 ---
 class="card recycle" data-layer="recycle">
        <div class="card-head"><div><h3>♻️ 재활용쓰레기</h3><p>종이·플라스틱·캔·병 등</p></div><span id="recycleBadge" class="badge">확인 중</span></div>
        <div id="recycleDays" class="days"></div>
        <div id="recycleDetail" class="detail"></div>
      </article>
    </div>
    <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수
--- 수거권역 #2 @473003 ---
     <div class="card-head"><div><h3>♻️ 재활용쓰레기</h3><p>종이·플라스틱·캔·병 등</p></div><span id="recycleBadge" class="badge">확인 중</span></div>
        <div id="recycleDays" class="days"></div>
        <div id="recycleDetail" class="detail"></div>
      </article>
    </div>
    <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
--- 수거권역 #3 @473191 ---
   <div id="recycleDetail" class="detail"></div>
      </article>
    </div>
    <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data
--- 수거권역 #4 @473539 ---
활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>
  </aside>

  <section class="map-wrap">
    <div id="map" class="map"></div>
    <div class="layer-control">
      <button class="layer-btn active" data-layer="general">생활</button>
      <button class="layer-btn" data-layer="recycle">재활용</button>
      <button class="layer-btn" data-layer="food">음식물</button>
    </div>
    <div id="vehicleFilter" class="vehicle-filter"></div>
    <div id="dayFilter" class="day-filter">
      <button class="active" data-day="전체">전체요일</button>
      <button data-day="월">월</button>
      <button data-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class=
--- 수거권역 #5 @474705 ---
ata-day="화">화</button>
      <button data-day="수">수</button>
      <button data-day="목">목</button>
      <button data-day="금">금</button>
    </div>
    <div id="structureControl" class="structure-control">
      <span class="structure-label">레이어</span>
      <button class="active" data-structure="zone">① 추정권역</button>
      <button data-structure="route">② 동지역 근거노선</button>
    </div>
    <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>
    <div id="adminPanel" class="admin-panel">
      <h4>수거권역 관리자 편집</h4>
      <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>
      <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>
      <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>
      <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>
      <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel">취소</button><button id="adminRebuild">자동권역 재표시</button></div>
      <div id="adminStatus" class="admin-status">저장된 수동 권역을 불러오는 중입니다.</div>
      <button id="adminExport" class="wide-btn">수동권역 JSON 내보내기</button>
      <button id="adminImport
--- 수거권역 #6 @587879 ---
"토성로48","내수로199","충청대로233"],"수":["발산로6-9"],"목":["율봉로95","주성로89","충청대로233","2순환로278-1","사뜸로49"],"금":["팔결로188"]}}};

// 읍·면은 법정리 면레이어로 처리하고, 동지역 노선선은 차량 전체 코스에서 실제 등장하는 동 권역을 허용합니다.
// 일자별 수동 추론 누락 때문에 도로가 사라지던 문제를 막기 위한 차량 단위 범위입니다.
// ② 직영 근거노선은 동지역 중심으로 유지하고, 새로 제공된 대행업체 노선은 청원구 대행구간에 한해 추가 표시합니다.
const ROUTE_DONG_SCOPE={
  general:{
    '3346':['율량사천동','오근장동'],
    '6244':['율량사천동','오근장동'],
    '6224':['오근장동','율량사천동'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '9090':['율량사천동','오근장동'],
    '0264':['오근장동','율량사천동']
  }
};
// 읍·면 ① 수거권역은 기존 원자료 기반 권역판정을 유지하기 위한 별도 범위입니다.
// 이 범위는 ② 근거노선 레이어/차량노선 선택에는 사용하지 않습니다.
const SERVICE_ZONE_ROUTE_SCOPE={
  general:{
    '3344':['오창읍','북이면'],
    '3346':['율량사천동','오근장동','내수읍'],
    '6244':['율량사천동','오근장동'],
    '6224':['내수읍','북이면','오근장동','율량사천동'],
    '6138':['내수읍','북이면'],
    '6157':['내덕2동','율량사천동']
  },
  recycle:{
    '6544':['내수읍','북이면'],
    '0263':['율량사천동','오근장동'],
    '0261':['내덕2동','율량사천동'],
    '0262':['오창읍','북이면'],
    '9090':['율량사천동','오근장동','내수읍'],
    '0264':['내수읍','북이면','오근장동','율량사천동']
  }
};
// 원본 코스표를 차량×요일 단위로 다시 감사한 동지역 범위입니다.
// 차량 단위 범위보다 우선 적용하여, 예를 들어 6244/0263 노선이 오근장동으로 번지는 것을 막습니다.
const ROUTE_DONG_DAY_SCOPE={
  general:{
    '6244':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']}
  },
  recycle:{
    '0263':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '9090':{월:['율량사천동'],화:['율량사천동','오근장동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '0264':{월:['율량사천동','오근장동'],화:['율량사천동','오근장동'],수:['율량사천동','오근장동'],목:['율량사천동','오근장동'],금:['오근장동']}
  }
};
function routeDongScope(type,vehicle,day=''){
  const cm=contractRouteMeta(vehicle);if(cm)return [canonicalDistrict(cm.district)];
  const exact=day&&ROUTE_DONG_DAY_SCOPE[type]?.[vehicl
--- 수거권역 #7 @602478 ---
lace_name||keyword;document.getElementById('landingInput').value=address;setLandingStatus('');moveTo(lat,lng,address)}else setLandingStatus('주소를 찾지 못했습니다. 청원구 도로명 주소를 조금 더 자세히 입력해주세요.',true)})})}
function reverseGeocode(lat,lng){geocoder.coord2Address(lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const dat
--- 수거권역 #8 @603224 ---
]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Arr
## syncServiceZoneVisibility count=8
--- syncServiceZoneVisibility #1 @610284 ---
  tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();
    });
  }catch(e){console.warn('수거권역 캐시 삭제 실패',e)}
}
async function hydrateCachedServiceZones(type){
  if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];
  serviceZoneCacheHydrated[type]=true;
  const cached=await getCachedServiceZones(type);
  if(!cached?.length)return false;
  clearServiceZoneOverlays(type);
  for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});
  rebuildManualZoneOverlays(type);
  serviceZoneBuilt[type]=true;
  syncServiceZoneVisibility(type);
  return true;
}
function serviceZoneOverlayMatch(type,ctx){
  const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');
  const riName=normalizeRi(ctx?.legalRi||'');
  const lat=+ctx?.lat,lng=+ctx?.lng;
  if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;
  const manual=manualZoneMatch(type,lat,lng);
  if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};
  const hits=(serviceZoneOverlays[type]||[]).filter(z=>{
    if(z.manual)return false;
    if(z.district&&canonicalDistrict(z.district)!==district)return false;
    if(riName && normalizeRi(z.riName||'')!==riName)return false;
    return pointInZonePolygon(lat,lng,z);
  });
  if(!hits.length)return null;
  hits.sort((a,b)=>(a.ambiguous?2:(a.estimated?1:0))-(b.ambiguous?2:(b.estimated?1:0)));
  const z=hits[0];
  const dongEstimated=DONG_ROUTE_DISTRICTS.has(district)&&!z.manual&&!z.routeBuffer;
  return {vehicle:z.vehicle,vehicles:[...(z.vehicles||[])],provider:z.provider||routeCompany(z.vehi
--- syncServiceZoneVisibility #2 @702518 ---
ght},{lat:top,lng:right},{lat:top,lng:left}],{
      district:region.district,riName:region.riName||'',vehicle:rect.group.vehicle,provider:rect.group.provider||routeCompany(rect.group.vehicle),days:rect.group.days,groupKey:rect.group.key,
      ambiguous:false,estimated:true,neighborRatio:+rect.neighborRatio||0,neighborSupport:+rect.neighborSupport||0,neighborTotal:+rect.neighborTotal||0,automatic:true,rural,gridMerged:true,estimateReason:'outside-confirmed-route'
    });
  }
}
function isContractRegion(region){return false}
async function buildServiceZones(type){
  if(serviceZoneBuilt[type]){syncServiceZoneVisibility(type);return}
  if(serviceZoneBuildPromises.has(type))return serviceZoneBuildPromises.get(type);
  const task=(async()=>{
    // 저장된 자동권역이 있으면 네트워크/재계산 없이 즉시 복원합니다.
    if(await hydrateCachedServiceZones(type))return;
    if(!serviceRegionLoadComplete)return;

    clearServiceZoneOverlays(type);
    const regions=[...serviceZoneRegions];
    let done=0;
    for(const region of regions){
      if(isUnconfirmedRi(type,region.district,region.riName||'')){done++;continue}
      if(RI_LAYER_DISTRICTS.has(region.district)){
        // v66: 내수·오창·북이는 법정리 경계 자체를 ① 수거권역으로 사용합니다.
        // 같은 리 안을 100m 격자로 다시 쪼개지 않습니다.
        await buildRuralRiServiceZone(type,region);
      }else{
        const groups=await serviceRouteGroups(type,region.district,region.riName||'');
        const usable=groups.filter(g=>g.items.length);
        if(!usable.length){
          // 근거노선 좌표가 없으면 동 전체를 임의 수거권역으로 채우지 않습니다.
        }else{
          await buildGridServiceZonesForRegion(type,region,usable);
        }
      }
      done++;
      // 계산되는 즉시 화면에 추가하되, 사용자에게 별도 '로딩' 상태를 표시하지 않습니다.
      if(activeLayer===type&&serviceLayerState.zone)syncServiceZoneVisibility(type);
      if(done
--- syncServiceZoneVisibility #3 @703672 ---
    // v66: 내수·오창·북이는 법정리 경계 자체를 ① 수거권역으로 사용합니다.
        // 같은 리 안을 100m 격자로 다시 쪼개지 않습니다.
        await buildRuralRiServiceZone(type,region);
      }else{
        const groups=await serviceRouteGroups(type,region.district,region.riName||'');
        const usable=groups.filter(g=>g.items.length);
        if(!usable.length){
          // 근거노선 좌표가 없으면 동 전체를 임의 수거권역으로 채우지 않습니다.
        }else{
          await buildGridServiceZonesForRegion(type,region,usable);
        }
      }
      done++;
      // 계산되는 즉시 화면에 추가하되, 사용자에게 별도 '로딩' 상태를 표시하지 않습니다.
      if(activeLayer===type&&serviceLayerState.zone)syncServiceZoneVisibility(type);
      if(done%5===0)await sleepMs(0);
    }
    rebuildManualZoneOverlays(type);
    serviceZoneBuilt[type]=true;
    syncServiceZoneVisibility(type);
    await putCachedServiceZones(type);
    if(activeLayer===type)updateMapBadge();
  })();
  serviceZoneBuildPromises.set(type,task);
  try{return await task}finally{serviceZoneBuildPromises.delete(type)}
}

function syncServiceZoneVisibility(type,forceRedraw=false){
  const provider=selectedVehicle[type]||'전체',day=activeDay||'전체';
  for(const t of ['general','recycle']){
    for(const z of (serviceZoneOverlays[t]||[])){
      const zoneVehicles=(z.vehicles||[]).length?z.vehicles:[z.vehicle];
      const providerOk=provider==='전체'||zoneVehicles.some(v=>routeSelectedBy(provider,v))||provider===(z.provider||'');
      const dayOk=day==='전체'||(z.days||[]).includes(day);
      const visible=t===type&&activeLayer===type&&serviceLayerState.zone&&providerOk&&dayOk;
      if(!visible){if(z.polygon?.getMap())z.polygon.setMap(null);continue}
      const contract=!!z.contractLayer;
      const color=z.manual?'#0f172a':(z.ambiguous?'#94a3b8':(contract?OCHANG_CONTRACT_COLOR:(day!=='전체'?(DAY_COLORS[day]||'#64748b')
--- syncServiceZoneVisibility #4 @703823 ---
se{
        const groups=await serviceRouteGroups(type,region.district,region.riName||'');
        const usable=groups.filter(g=>g.items.length);
        if(!usable.length){
          // 근거노선 좌표가 없으면 동 전체를 임의 수거권역으로 채우지 않습니다.
        }else{
          await buildGridServiceZonesForRegion(type,region,usable);
        }
      }
      done++;
      // 계산되는 즉시 화면에 추가하되, 사용자에게 별도 '로딩' 상태를 표시하지 않습니다.
      if(activeLayer===type&&serviceLayerState.zone)syncServiceZoneVisibility(type);
      if(done%5===0)await sleepMs(0);
    }
    rebuildManualZoneOverlays(type);
    serviceZoneBuilt[type]=true;
    syncServiceZoneVisibility(type);
    await putCachedServiceZones(type);
    if(activeLayer===type)updateMapBadge();
  })();
  serviceZoneBuildPromises.set(type,task);
  try{return await task}finally{serviceZoneBuildPromises.delete(type)}
}

function syncServiceZoneVisibility(type,forceRedraw=false){
  const provider=selectedVehicle[type]||'전체',day=activeDay||'전체';
  for(const t of ['general','recycle']){
    for(const z of (serviceZoneOverlays[t]||[])){
      const zoneVehicles=(z.vehicles||[]).length?z.vehicles:[z.vehicle];
      const providerOk=provider==='전체'||zoneVehicles.some(v=>routeSelectedBy(provider,v))||provider===(z.provider||'');
      const dayOk=day==='전체'||(z.days||[]).includes(day);
      const visible=t===type&&activeLayer===type&&serviceLayerState.zone&&providerOk&&dayOk;
      if(!visible){if(z.polygon?.getMap())z.polygon.setMap(null);continue}
      const contract=!!z.contractLayer;
      const color=z.manual?'#0f172a':(z.ambiguous?'#94a3b8':(contract?OCHANG_CONTRACT_COLOR:(day!=='전체'?(DAY_COLORS[day]||'#64748b'):serviceZoneColor(z.days,type))));
      const rural=!!z.rural||RI_LAYER_DISTRICTS.has(z.district||'');
      z.polygon.setOptions({
        fillColor:
--- syncServiceZoneVisibility #5 @704073 ---
 await buildGridServiceZonesForRegion(type,region,usable);
        }
      }
      done++;
      // 계산되는 즉시 화면에 추가하되, 사용자에게 별도 '로딩' 상태를 표시하지 않습니다.
      if(activeLayer===type&&serviceLayerState.zone)syncServiceZoneVisibility(type);
      if(done%5===0)await sleepMs(0);
    }
    rebuildManualZoneOverlays(type);
    serviceZoneBuilt[type]=true;
    syncServiceZoneVisibility(type);
    await putCachedServiceZones(type);
    if(activeLayer===type)updateMapBadge();
  })();
  serviceZoneBuildPromises.set(type,task);
  try{return await task}finally{serviceZoneBuildPromises.delete(type)}
}

function syncServiceZoneVisibility(type,forceRedraw=false){
  const provider=selectedVehicle[type]||'전체',day=activeDay||'전체';
  for(const t of ['general','recycle']){
    for(const z of (serviceZoneOverlays[t]||[])){
      const zoneVehicles=(z.vehicles||[]).length?z.vehicles:[z.vehicle];
      const providerOk=provider==='전체'||zoneVehicles.some(v=>routeSelectedBy(provider,v))||provider===(z.provider||'');
      const dayOk=day==='전체'||(z.days||[]).includes(day);
      const visible=t===type&&activeLayer===type&&serviceLayerState.zone&&providerOk&&dayOk;
      if(!visible){if(z.polygon?.getMap())z.polygon.setMap(null);continue}
      const contract=!!z.contractLayer;
      const color=z.manual?'#0f172a':(z.ambiguous?'#94a3b8':(contract?OCHANG_CONTRACT_COLOR:(day!=='전체'?(DAY_COLORS[day]||'#64748b'):serviceZoneColor(z.days,type))));
      const rural=!!z.rural||RI_LAYER_DISTRICTS.has(z.district||'');
      z.polygon.setOptions({
        fillColor:color,
        fillOpacity:z.manual?.28:(z.ambiguous?.18:(contract?.36:(rural?.30:.22))),
        strokeWeight:z.gridMerged?0:(z.manual?2:(contract?2:(rural?1:0))),
        strokeColor:z.manual?'#111827':color,
        strokeOpacity:z.gridMerged?0:(z
--- syncServiceZoneVisibility #6 @705870 ---
:(z.manual?.95:(contract?.86:(rural?.42:0)))
      });
      if(forceRedraw&&z.polygon.getMap()===map)z.polygon.setMap(null);
      if(z.polygon.getMap()!==map)z.polygon.setMap(map);
    }
  }
}
let serviceLayerRefreshSeq=0,serviceLayerHardRefresh=false;
function queueActiveServiceLayerRefresh(force=false){
  if(force)serviceLayerHardRefresh=true;
  const seq=++serviceLayerRefreshSeq;
  requestAnimationFrame(()=>{
    if(seq!==serviceLayerRefreshSeq)return;
    if(activeLayer!=='general'&&activeLayer!=='recycle')return;
    const hard=serviceLayerHardRefresh;serviceLayerHardRefresh=false;
    syncServiceZoneVisibility(activeLayer,hard);
  });
}
function syncBoundaryVisibility(){
  // v60: 행정/법정리 경계는 수거권역 계산용으로만 유지하고 지도에는 표시하지 않습니다.
  (zonePolygons.general||[]).forEach(i=>i.polygon?.setMap(null));
  (zonePolygons.recycle||[]).forEach(i=>i.polygon?.setMap(null));
  (zonePolygons.districtOutline||[]).forEach(i=>i.polygon?.setMap(null));
}
function setStructureLayer(key){
  if(!(key in serviceLayerState))return;serviceLayerState[key]=!serviceLayerState[key];
  document.querySelectorAll('#structureControl button').forEach(b=>b.classList.toggle('active',!!serviceLayerState[b.dataset.structure]));
  if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer);
}
function pointInSimplePolygon(lat,lng,points){return pointInRing([+lng,+lat],(points||[]).map(p=>[+p.lng,+p.lat]))}
function pointInZonePolygon(lat,lng,zone){
  const rings=(Array.isArray(zone?.hitRings)&&zone.hitRings.length)?zone.hitRings:((Array.isArray(zone?.rings)&&zone.rings.length)?zone.rings:[zone?.points||[]]);
  if(!rings.length||!pointInSimplePolygon(lat,lng,rings[0]))return false;
  for(let i=1;i<rings.length;i++)if(pointInSimplePolygon(lat,lng,rings[i]))return false;
  return true;
}
function 
--- syncServiceZoneVisibility #7 @711445 ---
[];if(!Array.isArray(manualServiceZones))manualServiceZones=[]}catch(e){manualServiceZones=[]}
}
function saveManualZones(){localStorage.setItem(ADMIN_ZONE_STORAGE_KEY,JSON.stringify(manualServiceZones));updateAdminStatus()}
function removeManualZoneOverlays(type){
  const keep=[];for(const z of serviceZoneOverlays[type]){if(z.manual){z.polygon?.setMap(null)}else keep.push(z)}serviceZoneOverlays[type]=keep;
}
function rebuildManualZoneOverlays(type){
  removeManualZoneOverlays(type);for(const z of allAdminZones().filter(z=>z.type===type))addServiceZonePolygon(type,z.points,{...z,manual:true});syncServiceZoneVisibility(type)
}
function updateAdminVehicleOptions(){
  const type=document.getElementById('adminType')?.value||activeLayer;const sel=document.getElementById('adminVehicle');if(!sel)return;
  const vals=providerList(type).filter(v=>/^\d+$/.test(v));sel.innerHTML=vals.map(v=>`<option value="${escapeHtml(v)}">${escapeHtml(v)}호</option>`).join('');
}
function updateAdminStatus(msg=''){
  const el=document.getElementById('adminStatus');if(!el)return;el.classList.toggle('drawing',adminDrawing);el.textContent=msg||(adminDrawing?`권역 그리는 중 · 지도 클릭 ${adminDraftPoints.length}개 점 · 완료 버튼으로 저장`:`이 브라우저에 저장된 수동 권역 ${manualServiceZones.length}개`)
}
function clearAdminDraft(){
  adminDraftLine?.setMap(null);adminDraftPolygon?.setMap(null);adminDraftLine=null;adminDraftPolygon=null;adminDraftPoints=[];adminDrawing=false;updateAdminStatus()
}
function redrawAdminDraft(){
  adminDraftLine?.setMap(null);adminDraftPolygon?.setMap(null);if(!adminDraftPoints.length)return;
  const path=adminDraftPoints.map(p=>new kakao.maps.LatLng(p.lat,p.lng));
  adminDraftLine=new kakao.maps.Polyline({path,strokeWeight:3,strokeColor:'#111827',strokeOpacity:.95,strokeStyle:'solid'});adminDraftLine.set
--- syncServiceZoneVisibility #8 @717143 ---
le('show',service);
  renderVehicleFilter();hideDongRouteOverlays();for(const t of ['general','recycle'])(serviceZoneOverlays[t]||[]).forEach(z=>z.polygon?.setMap(null));
  const seen=new Set();Object.values(zonePolygons).flat().forEach(item=>{if(!item?.polygon||seen.has(item.polygon))return;seen.add(item.polygon);item.polygon.setMap(null)});
  if(layer==='food'){
    zonePolygons.food.forEach(item=>item.polygon.setMap(map));zonePolygons.foodExclusion.forEach(item=>item.polygon.setMap(map));
  }else{
    syncBoundaryVisibility();
    if(serviceLayerState.zone)buildServiceZones(layer).then(()=>syncServiceZoneVisibility(layer)).catch(e=>console.warn('수거권역 생성 실패',e));
    if(serviceLayerState.route)ensureDongRoutesForSelection(layer);else hideDongRouteOverlays();
  }
  updateMapBadge();
}
function updateMapBadge(custom){
  const el=document.getElementById('mapBadge');const labels={general:'생활쓰레기',food:'음식물쓰레기',recycle:'재활용쓰레기'};
  if(activeLayer==='food'){
    el.innerHTML=`<strong>음식물쓰레기 수거·분류배출 레이어</strong><span>${custom||'수거 시행지역과 분류배출 제외지역을 면색으로 구분합니다.'}</span><div class="food-legend"><span class="legend-item"><i class="legend-dot legend-mwf"></i>시행지역 · 월·수·금</span><span class="legend-item"><i class="legend-dot legend-tts"></i>시행지역 · 화·목·일</span><span class="legend-item"><i class="legend-dot legend-excluded"></i>분류배출 제외지역</span></div>`;return;
  }
  const provider=selectedVehicle[activeLayer]||'전체',dayLabel=activeDay==='전체'?'전체요일':`${activeDay}요일`,providerLabel=provider==='전체'?'전체 수거주체':((provider==='제일환경'||provider==='현대환경')?`${provider} 대행`:`${provider}호 직영`);
  const state=[serviceLayerState.zone?'①수거권역':'',serviceLayerState.route?'②근거노선':''].filter(Boolean).join(' + ');
  el.innerHTML=`<strong>${labels[activeLayer]} · ${providerLabel} · ${dayLabel}</strong><span>${
## serviceZone count=65
--- serviceZone #1 @589163 ---
'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']}
  },
  recycle:{
    '0263':{월:['율량사천동'],화:['율량사천동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '9090':{월:['율량사천동'],화:['율량사천동','오근장동'],수:['율량사천동'],목:['율량사천동'],금:['율량사천동']},
    '0264':{월:['율량사천동','오근장동'],화:['율량사천동','오근장동'],수:['율량사천동','오근장동'],목:['율량사천동','오근장동'],금:['오근장동']}
  }
};
function routeDongScope(type,vehicle,day=''){
  const cm=contractRouteMeta(vehicle);if(cm)return [canonicalDistrict(cm.district)];
  const exact=day&&ROUTE_DONG_DAY_SCOPE[type]?.[vehicle]?.[day];
  return [...(exact||ROUTE_DONG_SCOPE[type]?.[vehicle]||[])];
}
function serviceZoneRouteScope(type,vehicle){
  const cm=contractRouteMeta(vehicle);if(cm)return cm.zoneEligible?[canonicalDistrict(cm.district)]:[];
  return [...(SERVICE_ZONE_ROUTE_SCOPE[type]?.[vehicle]||[])];
}

function sourceRoadTermsForSpec(spec){
  return [...(SOURCE_ROUTE_ROADS[spec?.type]?.[spec?.vehicle]?.[spec?.day]||[])];
}
function sourceAddressRangeTermsForSpec(spec){
  return [...(SOURCE_ROUTE_ADDRESS_RANGES[spec?.type]?.[spec?.vehicle]?.[spec?.day]||[])];
}

function norm(s){return String(s||'').replace(/[\s·.]/g,'')}
function canonicalDistrict(name){const n=norm(name);for(const [alias,canonical] of Object.entries(DISTRICT_ALIASES)){if(norm(alias)===n)return canonical}return name||''}
function normalizeRi(name){return String(name||'').replace(/\s+/g,'').trim()}
function getFoodExclusion(legalEmd,legalRi,riCode=''){
  const emd=canonicalDistrict(legalEmd); const ri=normalizeRi(legalRi);
  // 오창읍 화산리(華山, 빛화산) 법정리코드. 동명이리 花山과 구분해 제외지역을 명시적으로 유지합니다.
  if(emd==='오창읍'&&ri==='화산리'&&String(riCode)==='4311425322') return {excluded:true,label:'오창읍 화산리(華山)',reason:'음식물류 폐기물 분류배출 제외지역'};
  if(emd==='북이면') return {excluded:true,label:'북이면 전 지역',reason:'음식물류 폐기물 분류배출 제외지역'};
  const incl
--- serviceZone #2 @602583 ---
lng,address)}else setLandingStatus('주소를 찾지 못했습니다. 청원구 도로명 주소를 조금 더 자세히 입력해주세요.',true)})})}
function reverseGeocode(lat,lng){geocoder.coord2Address(lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시
--- serviceZone #3 @602634 ---
원구 도로명 주소를 조금 더 자세히 입력해주세요.',true)})})}
function reverseGeocode(lat,lng){geocoder.coord2Address(lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.vers
--- serviceZone #4 @602676 ---
nction reverseGeocode(lat,lng){geocoder.coord2Address(lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      con
--- serviceZone #5 @602730 ---
lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.ty
--- serviceZone #6 @603022 ---
stSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
     
--- serviceZone #7 @604719 ---
)continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneOverlays(type);
          for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});
          rebuildManualZoneOverlays(type);
          serviceZoneBuilt[type]=true;
          serviceZoneCacheHydrated[type]=true;
          hydrated=true;
        }
      }
    }
    return hydrated;
  }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false}
}
function exportCjWastePrecomputed(){
  const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];
  const routes=[];
  for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){
    const data={};
    for(const k of routeFields){
      if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';
      else if(item[k]!==undefined)data[k]=item[k];
    }
    data.distance=Number(item.roadResult?.distance)||0;
    routes.push({type,vehicle:item.vehicle,day:item.day,data});
  }
  const zones={general:[],recycle:[]};
  for(const type of ['general','recycle'])zones[type]=(serviceZoneOverlays[type]||[])
    .filter(z=>!z.manual&&Array.isArray(z.points)&&z.poin
--- serviceZone #8 @604758 ---
ay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneOverlays(type);
          for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});
          rebuildManualZoneOverlays(type);
          serviceZoneBuilt[type]=true;
          serviceZoneCacheHydrated[type]=true;
          hydrated=true;
        }
      }
    }
    return hydrated;
  }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false}
}
function exportCjWastePrecomputed(){
  const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];
  const routes=[];
  for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){
    const data={};
    for(const k of routeFields){
      if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';
      else if(item[k]!==undefined)data[k]=item[k];
    }
    data.distance=Number(item.roadResult?.distance)||0;
    routes.push({type,vehicle:item.vehicle,day:item.day,data});
  }
  const zones={general:[],recycle:[]};
  for(const type of ['general','recycle'])zones[type]=(serviceZoneOverlays[type]||[])
    .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)
    .map(z=>({
      poin
## evidence count=3
--- evidence #1 @662876 ---
:+p.lng,address:p.address||''};
    const district=routePointAnyDistrict(pt);
    if(!district||!CHEONGWON_ROUTE_DISTRICTS.has(district)){
      invalid.push({where,reason:'outside-cheongwon',district:district||'',lat:pt.lat,lng:pt.lng});
      return false;
    }
    if(!allowed.includes(district)){
      invalid.push({where,reason:'outside-vehicle-scope',district,lat:pt.lat,lng:pt.lng});
      return false;
    }
    return true;
  };
  (data?.segments||[]).forEach((seg,si)=>(seg||[]).forEach((p,pi)=>check(p,`segment:${si}:${pi}`)));
  (data?.zoneEvidencePoints||[]).forEach((p,pi)=>check(p,`evidence:${pi}`));
  return {ok:invalid.length===0,invalidCount:invalid.length,invalid:invalid.slice(0,12),allowed};
}
function sanitizeRouteDataToScope(spec,data){
  const allowed=routeDongScope(spec.type,spec.vehicle,spec.day);
  const segments=[];
  for(const seg of (data?.segments||[])){
    const clipped=clipRoutePathToDistricts(seg,allowed);
    for(const part of clipped){
      if(part.length>=2)segments.push(part.map(p=>({lat:+p.lat,lng:+p.lng})));
    }
  }
  const zoneEvidencePoints=(data?.zoneEvidencePoints||[]).filter(p=>{
    const d=routePointAnyDistrict(p);
    return d&&allowed.includes(d)&&routePointInsideCheongwon(p);
  }).map(p=>({lat:+p.lat,lng:+p.lng,name:p.name||'',address:p.address||'',term:p.term||'',district:routePointAnyDistrict(p)}));
  return {...data,segments,servicePoints:[],zoneEvidencePoints};
}
function existingDongRouteOverlay(spec){
  return (dongRouteOverlays[spec.type]||[]).find(x=>x.vehicle===spec.vehicle&&x.day===spec.day) || null;
}
function createDongRouteOverlay(spec,data){
  let segments=Array.isArray(data.segments)?data.segments:[];
  segments=segments
    .map(seg=>(seg||[]).filter(p=>Number.isFinite(+p.lat)&&Number.isFinite(+p.lng)))
  
--- evidence #2 @667029 ---
.forEach(pl=>pl.setOptions({strokeWeight:5,strokeOpacity:.9}));
  };
  const basisText=()=>{
    const api=roadRequestedCount?` · 도로망 ${roadMatchedCount}/${roadRequestedCount}`:'';
    const outside=outsideTerms.length?` · 읍·면/다른 권역 ${outsideTerms.length}개`:'';
    const blocked=outOfCheongwonTerms.length?` · 청원구 밖 차단 ${outOfCheongwonTerms.length}개`:'';
    const roadHandled=coveredRoadTerms.length+outsideRoadTerms.length;
    const roads=requiredRoadTerms.length?` · 원본 명시도로 ${roadHandled}/${requiredRoadTerms.length} (동선 ${coveredRoadTerms.length}·읍면 ${outsideRoadTerms.length})`:'';
    const evidence=zoneEvidencePoints.length?` · 권역근거점 ${zoneEvidencePoints.length}개`:'';
    return `원본 ${termsCount}개 항목 · 표시 수거구간 ${segments.length}개${evidence}${inferredMovementCount?` · 내수·북이 차량이동 추정 ${inferredMovementCount}구간`:''}${roads}${api}${outside}${blocked} · 미해결 ${missingTerms.length}개`;
  };
  const mouseover=e=>{
    emphasize();
    showZoneTooltip(e.latLng,`${spec.vehicle}호 · ${spec.day}요일`,`${districtLabel} 수거구간`,basisText());
  };
  const mousemove=e=>moveZoneTooltip(e.latLng);
  const mouseout=()=>{normalize();hideZoneTooltip()};
  const click=()=>{
    const original=String(spec.raw||'').replace(/\s+/g,' ').trim();
    const apiNote=roadRequestedCount?` · 실제 도로망 확인 ${roadMatchedCount}/${roadRequestedCount}항목`:'';
    const outsideNote=outsideTerms.length?` · 읍·면/다른 권역 처리: ${escapeHtml(outsideTerms.join(', '))}`:'';
    const blockedNote=outOfCheongwonTerms.length?` · 청원구 밖 검색결과 차단: ${escapeHtml(outOfCheongwonTerms.join(', '))}`:'';
    const outsideRoadNote=outsideRoadTerms.length?` · 읍·면 면레이어 처리 도로: ${escapeHtml(outsideRoadTerms.join(', '))}`:'';
    const addressRangeNote=addressRangeTerms.length?` · 도로주소형 구간: ${escapeHtml(addressRangeTerms.join(', '))}`:'';
    const
--- evidence #3 @667173 ---
hedCount}/${roadRequestedCount}`:'';
    const outside=outsideTerms.length?` · 읍·면/다른 권역 ${outsideTerms.length}개`:'';
    const blocked=outOfCheongwonTerms.length?` · 청원구 밖 차단 ${outOfCheongwonTerms.length}개`:'';
    const roadHandled=coveredRoadTerms.length+outsideRoadTerms.length;
    const roads=requiredRoadTerms.length?` · 원본 명시도로 ${roadHandled}/${requiredRoadTerms.length} (동선 ${coveredRoadTerms.length}·읍면 ${outsideRoadTerms.length})`:'';
    const evidence=zoneEvidencePoints.length?` · 권역근거점 ${zoneEvidencePoints.length}개`:'';
    return `원본 ${termsCount}개 항목 · 표시 수거구간 ${segments.length}개${evidence}${inferredMovementCount?` · 내수·북이 차량이동 추정 ${inferredMovementCount}구간`:''}${roads}${api}${outside}${blocked} · 미해결 ${missingTerms.length}개`;
  };
  const mouseover=e=>{
    emphasize();
    showZoneTooltip(e.latLng,`${spec.vehicle}호 · ${spec.day}요일`,`${districtLabel} 수거구간`,basisText());
  };
  const mousemove=e=>moveZoneTooltip(e.latLng);
  const mouseout=()=>{normalize();hideZoneTooltip()};
  const click=()=>{
    const original=String(spec.raw||'').replace(/\s+/g,' ').trim();
    const apiNote=roadRequestedCount?` · 실제 도로망 확인 ${roadMatchedCount}/${roadRequestedCount}항목`:'';
    const outsideNote=outsideTerms.length?` · 읍·면/다른 권역 처리: ${escapeHtml(outsideTerms.join(', '))}`:'';
    const blockedNote=outOfCheongwonTerms.length?` · 청원구 밖 검색결과 차단: ${escapeHtml(outOfCheongwonTerms.join(', '))}`:'';
    const outsideRoadNote=outsideRoadTerms.length?` · 읍·면 면레이어 처리 도로: ${escapeHtml(outsideRoadTerms.join(', '))}`:'';
    const addressRangeNote=addressRangeTerms.length?` · 도로주소형 구간: ${escapeHtml(addressRangeTerms.join(', '))}`:'';
    const missingRoadNote=missingRoadTerms.length?` · 미표시 도로: ${escapeHtml(missingRoadTerms.join(', '))}`:'';
    const missingNote=missingTerms.length?`
## routePolyline count=0
## bundledPrecomputed count=4
--- bundledPrecomputed #1 @476400 ---
iv>
      <button id="adminExport" class="wide-btn">수동권역 JSON 내보내기</button>
      <button id="adminImport" class="wide-btn">수동권역 JSON 가져오기</button><input id="adminImportFile" type="file" accept="application/json,.json" hidden>
      <button id="adminClear" class="wide-btn" style="color:#9f1239">수동권역 전체 삭제</button>
      <div class="admin-zone-note">현재 관리자 편집 저장은 이 브라우저의 localStorage에 보관됩니다. 전역 홈페이지 반영용으로는 JSON을 내보내 보관할 수 있습니다.</div>
    </div>
    <div id="mapBadge" class="map-badge"><strong>생활쓰레기 수거노선 레이어</strong><span>읍·면·동 경계와 수거노선을 불러오는 중입니다.</span></div>
  </section>
</main>

<script id="bundledPrecomputed" type="application/json">{}</script>
<script>
const KAKAO_KEY='a103b2ec4ae35a508696bd61dabc0d65';
// Kakao Mobility REST API 키는 HTML에 넣지 않습니다. Cloudflare Worker 프록시 주소만 설정하세요.
const ROUTE_PROXY_URL='https://cjwaste-route.yseong22.workers.dev/route'; // Kakao Mobility route proxy Worker
const DEFAULT={lat:36.6515,lng:127.4866};
const DAYS=['월','화','수','목','금','토','일'];

// v30: 직영·위탁 수거노선을 같은 레이어 체계에서 수거주체별로 표시하고 읍·면·동 외곽선만 강조
// 2025~2026 청주시 공식 공개자료를 토대로 정리한 청원구 1차 데이터
const WASTE_DATA={
  food:[
    {regions:['내수읍','오근장동'],days:['월','수','금'],pickup:'익일 03시부터',operator:'승일',phone:'043-294-6065',status:'verified'},
    {regions:['오창읍'],days:['화','목','일'],pickup:'익일 03시부터',operator:'승일',phone:'043-294-6065',status:'verified'},
    {regions:['율량사천동'],days:['월','수','금'],pickup:'익일 03시부터',operator:'승일',phone:'043-292-6062',status:'verified'},
    {regions:['우암동','내덕1동','내덕2동'],days:['화','목','일'],pickup:'익일 03시부터',operator:'승일',phone:'043-292-6062',status:'verified'},
    {regions:['북이면'],days:[],pickup:'',status:'excluded'}
  ],
  general:[
    {regions:['내덕1동'],housing:'single',operator:'현대환경',phone:'',days:[],status:'route_based'},
    {regions:['우암동'],housing:'sin
--- bundledPrecomputed #2 @603329 ---
 serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneO
--- bundledPrecomputed #3 @603400 ---
N_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneOverlays(type);
          for(const z of zones)addServiceZonePolygon(typ
--- bundledPrecomputed #4 @603680 ---
t manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneOverlays(type);
          for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});
          rebuildManualZoneOverlays(type);
          serviceZoneBuilt[type]=true;
          serviceZoneCacheHydrated[type]=true;
          hydrated=true;
        }
      }
    }
    return hydrated;
  }catch(e){con
## DONG_ROUTE_CACHE_VERSION count=5
--- DONG_ROUTE_CACHE_VERSION #1 @576780 ---
ROUTE_DISTRICTS=new Set(['우암동','내덕1동','내덕2동','오근장동','율량사천동']);
const CHEONGWON_ROUTE_DISTRICTS=new Set(['내수읍','오창읍','북이면','우암동','내덕1동','내덕2동','오근장동','율량사천동']);
const districtFeatureMap=new Map();
const riFeatureMap=new Map(); // 읍·면 주소의 리 내부 세부 코스 판정용
const dongRouteOverlays={general:[],recycle:[]};
const dongRouteBuiltKeys=new Set();
const routePlaceSearchCache=new Map();

// 동지역 수거노선 영구 캐시.
// 첫 방문에는 백그라운드에서 차량×요일 노선을 계산하고 IndexedDB에 저장합니다.
// 이후 같은 브라우저에서는 저장된 도로망 좌표를 즉시 사용하므로 차량/요일 전환이 빠릅니다.
const DONG_ROUTE_CACHE_DB='cjwaste-dong-route-cache';
const DONG_ROUTE_CACHE_STORE='routes';
const DONG_ROUTE_CACHE_VERSION='v75-naedeok2-apartment-only';
const dongRouteBuildPromises=new Map();
let dongRoutePreloadStarted=false;

function routeTextHash(s){
  let h=2166136261>>>0;
  for(const ch of String(s||'')){
    h^=ch.charCodeAt(0);
    h=Math.imul(h,16777619)>>>0;
  }
  return h.toString(36);
}
function routeCacheKey(spec){
  return `${DONG_ROUTE_CACHE_VERSION}|${spec.type}|${spec.vehicle}|${spec.day}|${routeTextHash(spec.raw)}`;
}
function openDongRouteCache(){
  return new Promise((resolve,reject)=>{
    if(!('indexedDB' in window)){resolve(null);return}
    const req=indexedDB.open(DONG_ROUTE_CACHE_DB,1);
    req.onupgradeneeded=()=>{
      const db=req.result;
      if(!db.objectStoreNames.contains(DONG_ROUTE_CACHE_STORE)){
        db.createObjectStore(DONG_ROUTE_CACHE_STORE,{keyPath:'key'});
      }
    };
    req.onsuccess=()=>resolve(req.result);
    req.onerror=()=>reject(req.error);
  });
}
async function deleteCachedDongRoute(spec){
  try{
    const db=await openDongRouteCache();
    if(!db)return;
    await new Promise(resolve=>{
      const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');
      tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(routeCacheKey(
--- DONG_ROUTE_CACHE_VERSION #2 @577128 ---
PlaceSearchCache=new Map();

// 동지역 수거노선 영구 캐시.
// 첫 방문에는 백그라운드에서 차량×요일 노선을 계산하고 IndexedDB에 저장합니다.
// 이후 같은 브라우저에서는 저장된 도로망 좌표를 즉시 사용하므로 차량/요일 전환이 빠릅니다.
const DONG_ROUTE_CACHE_DB='cjwaste-dong-route-cache';
const DONG_ROUTE_CACHE_STORE='routes';
const DONG_ROUTE_CACHE_VERSION='v75-naedeok2-apartment-only';
const dongRouteBuildPromises=new Map();
let dongRoutePreloadStarted=false;

function routeTextHash(s){
  let h=2166136261>>>0;
  for(const ch of String(s||'')){
    h^=ch.charCodeAt(0);
    h=Math.imul(h,16777619)>>>0;
  }
  return h.toString(36);
}
function routeCacheKey(spec){
  return `${DONG_ROUTE_CACHE_VERSION}|${spec.type}|${spec.vehicle}|${spec.day}|${routeTextHash(spec.raw)}`;
}
function openDongRouteCache(){
  return new Promise((resolve,reject)=>{
    if(!('indexedDB' in window)){resolve(null);return}
    const req=indexedDB.open(DONG_ROUTE_CACHE_DB,1);
    req.onupgradeneeded=()=>{
      const db=req.result;
      if(!db.objectStoreNames.contains(DONG_ROUTE_CACHE_STORE)){
        db.createObjectStore(DONG_ROUTE_CACHE_STORE,{keyPath:'key'});
      }
    };
    req.onsuccess=()=>resolve(req.result);
    req.onerror=()=>reject(req.error);
  });
}
async function deleteCachedDongRoute(spec){
  try{
    const db=await openDongRouteCache();
    if(!db)return;
    await new Promise(resolve=>{
      const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');
      tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(routeCacheKey(spec));
      tx.oncomplete=()=>resolve();
      tx.onerror=()=>resolve();
      tx.onabort=()=>resolve();
    });
  }catch(e){
    console.warn('수거노선 캐시 삭제 실패',e);
  }
}
async function getCachedDongRoute(spec){
  try{
    const db=await openDongRouteCache();
    if(!db)return null;
    const cached=await new Promise(resolve=>{
      const tx=db.
--- DONG_ROUTE_CACHE_VERSION #3 @602988 ---
lng)})}



let nearbyScheduleRequestSeq=0;
let zoneBuildPromise=null;

// 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다.
const serviceLayerState={zone:true,route:false};
const serviceZoneOverlays={general:[],recycle:[]};
const serviceZoneBuildPromises=new Map();
const serviceZoneBuilt={general:false,recycle:false};
const serviceZoneRegions=[];
let serviceRegionLoadComplete=false;
const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1';
const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용
const SERVICE_ZONE_CACHE_VERSION=`zone-v75-naedeok2-apartment-only|${DONG_ROUTE_CACHE_VERSION}`;
const serviceZoneCacheHydrated={general:false,recycle:false};
let manualServiceZones=[];
let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;

// 빌드 시 확정된 노선/수거권역 좌표를 index.html 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromC
--- DONG_ROUTE_CACHE_VERSION #4 @603840 ---
tml 안에 직접 내장합니다.
// 새 PC/새 브라우저에서도 IndexedDB 생성이나 길찾기 API 계산을 기다리지 않고 즉시 복원됩니다.
function bundledPrecomputedData(){
  try{
    const el=document.getElementById('bundledPrecomputed');
    if(!el)return null;
    const raw=String(el.textContent||'').trim();
    if(!raw||raw==='null')return null;
    return JSON.parse(raw);
  }catch(e){console.warn('내장 사전계산 데이터 읽기 실패',e);return null}
}
function hydrateBundledPrecomputedData(){
  const data=bundledPrecomputedData();
  if(!data)return false;
  let hydrated=false;
  try{
    // 노선 좌표는 기존 v65 캐시를 그대로 재사용합니다. v66은 수거권역 구조만 변경합니다.
    if(data.version===DONG_ROUTE_CACHE_VERSION){
      const specMap=new Map(allDongRouteSpecs().map(s=>[`${s.type}|${s.vehicle}|${s.day}`,s]));
      for(const r of (data.routes||[])){
        const spec=specMap.get(`${r.type}|${r.vehicle}|${r.day}`);
        if(!spec||existingDongRouteOverlay(spec))continue;
        createDongRouteOverlay(spec,{...(r.data||{}),fromCache:true,fromBundle:true});
      }
      hydrated=true;
    }
    // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.
    if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){
      for(const type of ['general','recycle']){
        const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];
        if(zones.length){
          clearServiceZoneOverlays(type);
          for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});
          rebuildManualZoneOverlays(type);
          serviceZoneBuilt[type]=true;
          serviceZoneCacheHydrated[type]=true;
          hydrated=true;
        }
      }
    }
    return hydrated;
  }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false}
}
function exportCjWastePrecomputed(){
  const routeFields=['segments','zoneEvidencePoints','termsCount','pointsC
--- DONG_ROUTE_CACHE_VERSION #5 @606955 ---
ider:z.provider||routeCompany(z.vehicle),
      vehicles:[...(z.vehicles||[])],plate:z.plate||'',days:[...(z.days||[])],groupKey:z.groupKey||'',ambiguous:!!z.ambiguous,estimated:!!z.estimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,
      automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized
    }));
  return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones};
}
window.exportCjWastePrecomputed=exportCjWastePrecomputed;
window.__CJWASTE_PRECOMPUTE_READY=false;


function serviceZoneCacheKey(type){
  return `${SERVICE_ZONE_CACHE_VERSION}|${type}`;
}
async function getCachedServiceZones(type){
  try{
    const db=await openDongRouteCache();
    if(!db)return null;
    return await new Promise(resolve=>{
      const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');
      const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));
      req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);
      req.onerror=()=>resolve(null);
    });
  }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null}
}
async function putCachedServiceZones(type){
  try{
    const zones=(serviceZoneOverlays[type]||[])
      .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)
      .map(z=>({
        points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings