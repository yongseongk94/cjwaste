
### SERVICE_ZONE_CACHE_VERSION
Seq=0; let zoneBuildPromise=null;  // 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('bundledPrecomputed');     if(!el)return null;     const raw=String(el.textContent||'').trim();     if(!raw||raw==='null')return null;     return JSON.parse(raw);   }catch(e){console.warn('사전계산 데이터 읽기 실패',e);return null} } async function loadExternalBundledPrecomput
t audit=finalRouteCoordinateAudit(spec,bundleRouteData);         if(!audit.ok){           console.warn('사전계산 노선 scope 불일치로 복원을 건너뜁니다.',r.type,r.vehicle,r.day,audit);           continue;         }         createDongRouteOverlay(spec,{...bundleRouteData,fromCache:true,fromBundle:true});         putCachedDongRoute(spec,bundleRouteData).catch(e=>console.warn('사전계산 노선 IndexedDB 저장 실패',spec,e));       }       hydrated=true;     }     // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','second
ey||'',ambiguous:!!z.ambiguous,estimated:!!z.estimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,       automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized     }));   return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.
.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized     }));   return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.h

### serviceZone
 [...(SERVICE_ZONE_ROUTE_SCOPE[type]?.[vehicle]||[])];   return [...new Set(src.map(canonicalDistrict).filter(d=>RURAL_ROUTE_LAYER_DISTRICTS.has(d)))]; } function routeAllowedDistricts(spec){   const src=Array.isArray(spec?.districts)&&spec.districts.length?spec.districts:routeDongScope(spec?.type,spec?.vehicle,spec?.day);   return [...new Set(src.map(canonicalDistrict).filter(Boolean))]; } function routeScopeSignature(spec){return routeAllowedDistricts(spec).slice().sort().join('|');} function serviceZoneRouteScope(type,vehicle){   const cm=contractRouteMeta(vehicle);if(cm)return cm.zoneEligible?[canonicalDistrict(cm.district)]:[];   return [...(SERVICE_ZONE_ROUTE_SCOPE[type]?.[vehicle]||[])]; }  function sourceRoadTermsForSpec(spec){   return [...(SOURCE_ROUTE_ROADS[spec?.type]?.[spec?.vehicle]?.[spec?.day]||[])]; } function sourceAddressRangeTermsForSpec(spec){   return [...(SOURCE_ROUTE_ADDRESS_RANGES[spec?.type]?.[spec?.vehicle]?.[spec?.day]||[])]; }  function norm(s){return String(s||'').replace(/[\s·.]/g,'')} function canonicalDistrict(name){const n=norm(name);for(const [alias,canonical] of Object.entries(DISTRICT_ALIASES)){if(norm(alias)===n)return canonical}return name||''} function normalizeRi(name){return String(name||'').replace(/\s+/g,'').trim()} function getFoodExclusion(legalEmd,legalRi,riCode=''){   const emd=canonicalDistrict(legalEmd); const ri=normalizeRi(legalRi);   //
 reverseGeocode(lat,lng){geocoder.coord2Address(lng,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}    let nearbyScheduleRequestSeq=0; let zoneBuildPromise=null;  // 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledP
,lat,(r,s)=>{const address=(s===kakao.maps.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}    let nearbyScheduleRequestSeq=0; let zoneBuildPromise=null;  // 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExterna
.services.Status.OK&&r.length)?(r[0].road_address?.address_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}    let nearbyScheduleRequestSeq=0; let zoneBuildPromise=null;  // 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('b
ress_name||r[0].address?.address_name):'지도에서 선택한 위치';updateLocation(address,lat,lng);document.getElementById('landingInput').value=address;resolveDistrict(lat,lng)})}    let nearbyScheduleRequestSeq=0; let zoneBuildPromise=null;  // 2단계 구조: ① 자동/수동 수거권역 ② 원본 수거노선. 행정경계는 계산용으로만 사용하고 지도에는 표시하지 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('bundledPrecomputed');     if(!el)return null;     const
 않습니다. const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('bundledPrecomputed');     if(!el)return null;     const raw=String(el.textContent||'').trim();     if(!raw||raw==='null')return null;     return JSON.parse(raw);   }catch(e){console.warn('사전계산 데이터 읽기 실패',e);return null} } async function loadExternalBundledPrecomputedData(){   if(bundledPrecomputedExternal)return bundledPrecomputedExternal;   con
ceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('bundledPrecomputed');     if(!el)return null;     const raw=String(el.textContent||'').trim();     if(!raw||raw==='null')return null;     return JSON.parse(raw);   }catch(e){console.warn('사전계산 데이터 읽기 실패',e);return null} } async function loadExternalBundledPrecomputedData(){   if(bundledPrecomputedExternal)return bundledPrecomputedExternal;   const inline=bundledPrecomputedData();   if(inline)return inline;   i
 }         createDongRouteOverlay(spec,{...bundleRouteData,fromCache:true,fromBundle:true});         putCachedDongRoute(spec,bundleRouteData).catch(e=>console.warn('사전계산 노선 IndexedDB 저장 실패',spec,e));       }       hydrated=true;     }     // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];   const routes=[];   for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){ 
omCache:true,fromBundle:true});         putCachedDongRoute(spec,bundleRouteData).catch(e=>console.warn('사전계산 노선 IndexedDB 저장 실패',spec,e));       }       hydrated=true;     }     // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];   const routes=[];   for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){     const data={};     for(const k of routeFields){       if(
    }       hydrated=true;     }     // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];   const routes=[];   for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){     const data={};     for(const k of routeFields){       if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';       else if(item[k]!==undefined)data[k]=item[k];     }     data.distance
 v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];   const routes=[];   for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){     const data={};     for(const k of routeFields){       if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';       else if(item[k]!==undefined)data[k]=item[k];     }     data.distance=Number(item.roadResult?.distance)||0; 
  if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';       else if(item[k]!==undefined)data[k]=item[k];     }     data.distance=Number(item.roadResult?.distance)||0;     routes.push({       type,vehicle:item.vehicle,day:item.day,       districts:[...routeAllowedDistricts(item)],       scopeKind:item.scopeKind||'',       scopeSignature:routeScopeSignature(item),       data     });   }   const zones={general:[],recycle:[]};   for(const type of ['general','recycle'])zones[type]=(serviceZoneOverlays[type]||[])     .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)     .map(z=>({       points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points])).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),district:z.district||'',riName:z.riName||'',vehicle:z.vehicle||'',provider:z.provider||routeCompany(z.vehicle),       vehicles:[...(z.vehicles||[])],plate:z.plate||'',days:[...(z.days||[])],groupKey:z.groupKey||'',ambiguous:!!z.ambiguous,estimated:!!z.estimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,       automatic:true,sourceOnly:!!z.sourceOnly,riGroupe
|0,       automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized     }));   return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>(
yer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized     }));   return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isA
oISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points])).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),         district:z.district||'',riName:z.riName||'',vehicle:z.vehicle||'',provider:z.provider||routeCompany(z.vehicle),         vehicles:[...(z.vehicles||[])],pl
rviceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points])).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),         district:z.district||'',riName:z.riName||'',vehicle:z.vehicle||'',provider:z.provider||routeCompany(z.vehicle),         vehicles:[...(z.vehicles||[])],plate:z.plate||'',days:[...(z.days||[])],groupKey:z.groupKey||'',ambiguous:!!z.ambiguous,estimated:!!z.estimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTota
openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points])).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),         district:z.district||'',riName:z.riName||'',vehicle:z.vehicle||'',provider:z.provider||routeCompany(z.vehicle),         vehicles:[...(z.vehicles||[])],plate:z.plate||'',days:[...(z.days||[])],groupKey:z.groupKey||'',ambiguous:!!z.ambiguous,estimated:!!z.estimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,         automatic:true,sourceOnly:!!z.
tal||0,         automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized       }));     if(!zones.length)return;     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).put({key:serviceZoneCacheKey(type),savedAt:Date.now(),zones});       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlay
d,polygonized:!!z.polygonized       }));     if(!zones.length)return;     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).put({key:serviceZoneCacheKey(type),savedAt:Date.now(),zones});       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return tru
objectStore(DONG_ROUTE_CACHE_STORE).put({key:serviceZoneCacheKey(type),savedAt:Date.now(),zones});       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Num
atch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days
역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinit
 deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneS
){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[]
it openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',dist
e=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};   const hits=(serviceZoneOverlays[type]||[]).filter(z
dwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};   const hits=(serviceZoneOverlays[type]||[]).filter(z=>{     if(z.manual)return false;     if(z.district&&canonical
.delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};   const hits=(serviceZoneOverlays[type]||[]).filter(z=>{     if(z.manual)return false;     if(z.district&&canonicalDistrict(z.district)!==district)return false;     if(r
>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};   const hits=(serviceZoneOverlays[type]||[]).filter(z=>{     if(z.manual)return false;     if(z.district&&canonicalDistrict(z.district)!==district)return false;     if(riName && normalizeRi(z.riName||'')!==riName)return false;     return pointInZonePolygon(lat,lng,z);   });   i
sole.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,riName};   const hits=(serviceZoneOverlays[type]||[]).filter(z=>{     if(z.manual)return false;     if(z.district&&canonicalDistrict(z.district)!==district)return false;     if(riName && normalizeRi(z.riName||'')!==riName)return false;     return pointInZonePolygon(lat,lng,z);   });   if(!hits.length)return null;   hits.

### zoneCache
const serviceLayerState={zone:true,route:true}; const serviceZoneOverlays={general:[],recycle:[]}; const serviceZoneBuildPromises=new Map(); const serviceZoneBuilt={general:false,recycle:false}; const serviceZoneRegions=[]; let serviceRegionLoadComplete=false; const ADMIN_ZONE_STORAGE_KEY='cjwaste-admin-service-zones-v1'; const PUBLISHED_SERVICE_ZONES=[]; // 추후 검증 완료 권역을 코드에 영구 반영할 때 사용 const SERVICE_ZONE_CACHE_VERSION=`zone-v77-rural-estimated-regions|${DONG_ROUTE_CACHE_VERSION}`; const serviceZoneCacheHydrated={general:false,recycle:false}; let manualServiceZones=[]; let adminMode=false,adminDrawing=false,adminDraftPoints=[],adminDraftLine=null,adminDraftPolygon=null;  // 빌드 시 확정된 노선 좌표는 외부 정적 JSON으로 분리합니다. // 기존 브라우저는 IndexedDB를 우선 사용하고, 새 브라우저만 외부 사전계산 데이터를 1회 받아 캐시에 저장합니다. let bundledPrecomputedExternal=null; let bundledPrecomputedExternalPromise=null; let bundledPrecomputedHydrated=false; function bundledPrecomputedData(){   try{     if(bundledPrecomputedExternal)return bundledPrecomputedExternal;     const el=document.getElementById('bundledPrecomputed');     if(!el)return null;     const raw=String(el.textContent||'').trim();     if(!raw||raw==='null')return null;     return JSON.parse(raw);   }catch(e){console.warn('사전계산 데이터 읽기 실패',e);return null} } async function loadExternalBundledPrecomputedData(){   if(bundledPrecomputedExternal)return bundledPrecomputedExternal;   const in
게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다.     if(data.zoneVersion===SERVICE_ZONE_CACHE_VERSION){       for(const type of ['general','recycle']){         const zones=Array.isArray(data.zones?.[type])?data.zones[type]:[];         if(zones.length){           clearServiceZoneOverlays(type);           for(const z of zones)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true,fromBundle:true});           rebuildManualZoneOverlays(type);           serviceZoneBuilt[type]=true;           serviceZoneCacheHydrated[type]=true;           hydrated=true;         }       }     }     return hydrated;   }catch(e){console.warn('내장 사전계산 데이터 복원 실패',e);return false} } function exportCjWastePrecomputed(){   const routeFields=['segments','zoneEvidencePoints','termsCount','pointsCount','serviceTermCount','missingTerms','outsideTerms','outOfCheongwonTerms','rangeTerms','requiredRoadTerms','coveredRoadTerms','missingRoadTerms','outsideRoadTerms','addressRangeTerms','secondaryText','secondaryKind','districtLabel','roadMatchedCount','roadRequestedCount'];   const routes=[];   for(const type of ['general','recycle'])for(const item of (dongRouteOverlays[type]||[])){     const data={};     for(const k of routeFields){       if(k==='districtLabel')data[k]=item.district||item.districtLabel||'';       else if(item[k]!==undefined)data[k]=item[k];     }     data.distance=Number(item.roadResult?.distance)||0;     r
   automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized     }));   return {version:DONG_ROUTE_CACHE_VERSION,zoneVersion:SERVICE_ZONE_CACHE_VERSION,generatedAt:new Date().toISOString(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:
ing(),routes,zones}; } window.exportCjWastePrecomputed=exportCjWastePrecomputed; window.__CJWASTE_PRECOMPUTE_READY=false;   function serviceZoneCacheKey(type){   return `${SERVICE_ZONE_CACHE_VERSION}|${type}`; } async function getCachedServiceZones(type){   try{     const db=await openDongRouteCache();     if(!db)return null;     return await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readonly');       const req=tx.objectStore(DONG_ROUTE_CACHE_STORE).get(serviceZoneCacheKey(type));       req.onsuccess=()=>resolve(Array.isArray(req.result?.zones)?req.result.zones:null);       req.onerror=()=>resolve(null);     });   }catch(e){console.warn('수거권역 캐시 읽기 실패',e);return null} } async function putCachedServiceZones(type){   try{     const zones=(serviceZoneOverlays[type]||[])       .filter(z=>!z.manual&&Array.isArray(z.points)&&z.points.length>=3)       .map(z=>({         points:z.points.map(p=>({lat:+p.lat,lng:+p.lng})),rings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points]).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),hitRings:(Array.isArray(z.hitRings)&&z.hitRings.length?z.hitRings:(Array.isArray(z.rings)&&z.rings.length?z.rings:[z.points])).map(r=>r.map(p=>({lat:+p.lat,lng:+p.lng}))),         district:z.district||'',riName:z.riName||'',vehicle:z.vehicle||'',provider:z.provider||routeCompany(z.vehicle),         vehicles:[...(z.vehicles||[])],plate:z
         automatic:true,sourceOnly:!!z.sourceOnly,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized       }));     if(!zones.length)return;     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).put({key:serviceZoneCacheKey(type),savedAt:Date.now(),zones});       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(typ
tore(DONG_ROUTE_CACHE_STORE).put({key:serviceZoneCacheKey(type),savedAt:Date.now(),zones});       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 저장 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.i
 실패',e)} } async function deleteCachedServiceZones(type){   try{     const db=await openDongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,mat
DongRouteCache();if(!db)return;     await new Promise(resolve=>{       const tx=db.transaction(DONG_ROUTE_CACHE_STORE,'readwrite');       tx.objectStore(DONG_ROUTE_CACHE_STORE).delete(serviceZoneCacheKey(type));       tx.oncomplete=()=>resolve();tx.onerror=()=>resolve();tx.onabort=()=>resolve();     });   }catch(e){console.warn('수거권역 캐시 삭제 실패',e)} } async function hydrateCachedServiceZones(type){   if(serviceZoneCacheHydrated[type]||serviceZoneBuilt[type])return serviceZoneBuilt[type];   serviceZoneCacheHydrated[type]=true;   const cached=await getCachedServiceZones(type);   if(!cached?.length)return false;   clearServiceZoneOverlays(type);   for(const z of cached)addServiceZonePolygon(type,z.points,{...z,automatic:true,fromCache:true});   rebuildManualZoneOverlays(type);   serviceZoneBuilt[type]=true;   syncServiceZoneVisibility(type);   return true; } function serviceZoneOverlayMatch(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   const riName=normalizeRi(ctx?.legalRi||'');   const lat=+ctx?.lat,lng=+ctx?.lng;   if(!Number.isFinite(lat)||!Number.isFinite(lng))return null;   const manual=manualZoneMatch(type,lat,lng);   if(manual)return {vehicle:manual.vehicle,provider:manual.provider||routeCompany(manual.vehicle),days:[...(manual.days||[])],distanceKm:Infinity,matchBasis:'manual-zone',zoneStatus:'manual',matchedRoads:[],note:manual.note||'',district,
ementById('adminType').addEventListener('change',updateAdminVehicleOptions); document.getElementById('adminDraw').addEventListener('click',startAdminDrawing); document.getElementById('adminFinish').addEventListener('click',finishAdminDrawing); document.getElementById('adminCancel').addEventListener('click',clearAdminDraft); document.getElementById('adminRebuild').addEventListener('click',async()=>{for(const t of ['general','recycle']){clearServiceZoneOverlays(t);serviceZoneBuilt[t]=false;serviceZoneCacheHydrated[t]=false;await deleteCachedServiceZones(t)}await buildServiceZones(activeLayer);setLayer(activeLayer);updateAdminStatus('자동권역을 다시 계산해 저장했습니다.')}); document.getElementById('adminExport').addEventListener('click',exportAdminZones); document.getElementById('adminImport').addEventListener('click',()=>document.getElementById('adminImportFile').click()); document.getElementById('adminImportFile').addEventListener('change',e=>{if(e.target.files?.[0])importAdminZones(e.target.files[0]);e.target.value=''}); document.getElementById('adminClear').addEventListener('click',()=>{if(confirm('이 브라우저에 저장된 수동 수거권역을 모두 삭제할까요?')){manualServiceZones=[];saveManualZones();for(const t of ['general','recycle'])rebuildManualZoneOverlays(t);setLayer(activeLayer)}}); loadSdk(); </script> </body> </html>   

### hydrateBundledPrecomputedData
hrow new Error('HTTP '+res.status);       const data=await res.json();       bundledPrecomputedExternal=data;       return data;     }catch(e){       console.warn('외부 사전계산 데이터 로드 실패',e);       return null;     }finally{       bundledPrecomputedExternalPromise=null;     }   })();   return bundledPrecomputedExternalPromise; } async function hydrateExternalBundledPrecomputedData(){   if(bundledPrecomputedHydrated)return true;   const data=await loadExternalBundledPrecomputedData();   const ok=data?hydrateBundledPrecomputedData(data):false;   if(ok)bundledPrecomputedHydrated=true;   return ok; } function bundleRouteRecordSpec(record,specs){   const candidates=(specs||[]).filter(s=>s.type===record?.type&&s.vehicle===record?.vehicle&&s.day===record?.day);   if(!candidates.length)return null;    // v79 이후 번들은 담당권역까지 저장하므로 정확히 같은 scope를 우선 복원합니다.   const savedScope=String(record?.scopeSignature||'').trim();   if(savedScope){     const exact=candidates.find(s=>routeScopeSignature(s)===savedScope);     if(exact)return exact;   }   if(Array.isArray(record?.districts)&&record.districts.length){     const wanted=[...new Set(record.districts.map(canonicalDistrict).filter(Boolean))].sort().join('|');     const exact=candidates.find(s=>routeScopeSignature(s)===wanted);     if(exact)return exact;   }    // 구형 v76 번들은 scope 메타데이터가 없었습니다.   // 당시 base 노선과 오창 전용 rural 노선 중 실제 districtLabel과 맞는 후보를 찾고,   // Stage 3에서 뒤늦게 추가된 내
 rural 스펙에 예전 base 노선을 잘못 붙이지 않습니다.   const labels=String(record?.data?.districtLabel||'')     .split(/[·,]/).map(canonicalDistrict).filter(Boolean);   const compatible=candidates.filter(s=>{     const allowed=routeAllowedDistricts(s);     return !labels.length||labels.every(d=>allowed.includes(d));   });   if(compatible.length===1)return compatible[0];   const base=(compatible.length?compatible:candidates).find(s=>s.scopeKind==='base');   return base||(compatible[0]||candidates[0]); } function hydrateBundledPrecomputedData(dataOverride=null){   const data=dataOverride||bundledPrecomputedData();   if(!data)return false;   let hydrated=false;   try{     if(data.version===DONG_ROUTE_CACHE_VERSION){       const specs=allDongRouteSpecs();       for(const r of (data.routes||[])){         const spec=bundleRouteRecordSpec(r,specs);         if(!spec||existingDongRouteOverlay(spec))continue;         const bundleRouteData={...(r.data||{})};         const audit=finalRouteCoordinateAudit(spec,bundleRouteData);         if(!audit.ok){           console.warn('사전계산 노선 scope 불일치로 복원을 건너뜁니다.',r.type,r.vehicle,r.day,audit);           continue;         }         createDongRouteOverlay(spec,{...bundleRouteData,fromCache:true,fromBundle:true});         putCachedDongRoute(spec,bundleRouteData).catch(e=>console.warn('사전계산 노선 IndexedDB 저장 실패',spec,e));       }       hydrated=true;     }     // v65의 잘게 쪼갠 읍·면 권역은 v66에서 절대 복원하지 않습니다
.event.addListener(map,'click',handleMapClick);   kakao.maps.event.addListener(map,'zoom_start',()=>{serviceLayerHardRefresh=true;queueActiveServiceLayerRefresh(false)});   kakao.maps.event.addListener(map,'zoom_changed',()=>queueActiveServiceLayerRefresh(false));   kakao.maps.event.addListener(map,'idle',()=>queueActiveServiceLayerRefresh(serviceLayerHardRefresh));   window.addEventListener('scroll',()=>queueActiveServiceLayerRefresh(false),{passive:true});   loadManualZones();   const bundled=hydrateBundledPrecomputedData();   if(!bundled){hydrateCachedServiceZones('general').then(()=>setLayer('general'));hydrateCachedServiceZones('recycle');}   setLayer('general');   // 내장 좌표는 즉시 렌더링하고, 행정경계/누락분 점검은 뒤에서 진행합니다.   scheduleDongRoutePreload();   zoneBuildPromise=buildZonePolygons();   document.getElementById('landingInput').focus(); } function loadSdk(){const s=document.createElement('script');s.src=`https://dapi.kakao.com/v2/maps/sdk.js?appkey=${KAKAO_KEY}&libraries=services&autoload=false`;s.onload=()=>kakao.maps.load(init);s.onerror=()=>setLandingStatus('카카오맵 API를 불러오지 못했습니다. 등록 도메인을 확인해주세요.',true);document.head.appendChild(s)}  document.getElementById('landingForm').addEventListener('submit',e=>{e.preventDefault();if(!geocoder){setLandingStatus('지도를 준비 중입니다. 잠시 후 다시 검색해주세요.',true);return}searchAddress(document.getElementById('landingInput').value)}); document.getElementById('backBtn').addEventListener('

### ensureService
(z.estimated||dongEstimated)?'estimated':(z.ambiguous?'boundary':'inferred'),estimated:!!z.estimated||dongEstimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,matchedRoads:[],district,riName,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized}; } async function ensureServiceZoneMatch(type,ctx){   let match=serviceZoneOverlayMatch(type,ctx);if(match)return match;   if(!serviceZoneBuilt[type]){     const hydrated=await hydrateCachedServiceZones(type);     if(hydrated){match=serviceZoneOverlayMatch(type,ctx);if(match)return match}     await buildServiceZones(type);     match=serviceZoneOverlayMatch(type,ctx);   }   return match; }  function geoPointKm(lat,lng){   return {x:+lng*88,y:+lat*111}; } function pointSegmentDistanceKm(lat,lng,a,b){   if(!a||!b)return Infinity;   const p=geoPointKm(lat,lng), p1=geoPointKm(a.lat,a.lng), p2=geoPointKm(b.lat,b.lng);   const vx=p2.x-p1.x, vy=p2.y-p1.y;   const wx=p.x-p1.x, wy=p.y-p1.y;   const vv=vx*vx+vy*vy;   let t=vv>0?(wx*vx+wy*vy)/vv:0;   t=Math.max(0,Math.min(1,t));   return Math.hypot(p.x-(p1.x+t*vx),p.y-(p1.y+t*vy)); } const MAX_ADDRESS_ROUTE_DISTANCE_KM=0.020; function routeMatchLimitKm(district){const d=canonical
f ['general','recycle']){     const exact=contractExactScheduleMatch(type,ctx);if(exact){renderContractExactSchedule(type,exact,ctx);continue}     if(isUnconfirmedRi(type,district,ctx.legalRi||''))continue;     if(RURAL_ROUTE_LAYER_DISTRICTS.has(district)){       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);       else renderNoNearbyRouteSchedule(type,district);       continue;     }     if(RURAL_INFERRED_ROUTE_DISTRICTS.has(district)){       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length)renderNearbyRouteSchedule(type,near,ctx);       else if(!renderRestoredRuralRiSchedule(type,ctx))renderNoNearbyRouteSchedule(type,district);       continue;     }     if(DONG_ROUTE_DISTRICTS.has(district)){       // 확실한 표시 수거노선 20m 이내가 최우선입니다. 그 밖의 빈 공간에서만 추정권역을 사용합니다.       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleReques
RouteSchedule(type,near,ctx);       else if(!renderRestoredRuralRiSchedule(type,ctx))renderNoNearbyRouteSchedule(type,district);       continue;     }     if(DONG_ROUTE_DISTRICTS.has(district)){       // 확실한 표시 수거노선 20m 이내가 최우선입니다. 그 밖의 빈 공간에서만 추정권역을 사용합니다.       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);       else renderNoNearbyRouteSchedule(type,district);       continue;     }     // 오창읍은 기존 리 단위/대행 확정규칙을 유지합니다.     const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});     if(seq!==nearbyScheduleRequestSeq)return;     if(match)renderServiceZoneSchedule(type,match,ctx);     else renderNoNearbyRouteSchedule(type,district);   }   if(seq!==nearbyScheduleRequestSeq)return;   renderVehicleFilter();   if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer); }  function renderVehicleFilter(){   const el=document.getElementById('vehicleFilter');   const serviceLayer=activeLayer==='general'||activeLayer==='recycle';   el.classList.toggle('show',serv
});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);       else renderNoNearbyRouteSchedule(type,district);       continue;     }     // 오창읍은 기존 리 단위/대행 확정규칙을 유지합니다.     const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});     if(seq!==nearbyScheduleRequestSeq)return;     if(match)renderServiceZoneSchedule(type,match,ctx);     else renderNoNearbyRouteSchedule(type,district);   }   if(seq!==nearbyScheduleRequestSeq)return;   renderVehicleFilter();   if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer); }  function renderVehicleFilter(){   const el=document.getElementById('vehicleFilter');   const serviceLayer=activeLayer==='general'||activeLayer==='recycle';   el.classList.toggle('show',serviceLayer);   if(!serviceLayer){el.innerHTML='';return}   const current=selectedVehicle[activeLayer]||'전체';   const buttons=['전체',...providerList(activeLayer)];   const buttonLabel=v=>v==='전체'?'전체 노선':((v==='제일환경'||v==='현대환경')?`${v}(대행)`:v+'호');   el.innerHTML=`<span class="vehicle-label">수거주체</span>`+buttons.map(v=>`<button type="button" data-vehic
   for(let i=1;i<rings.length;i++)if(pointInSimplePolygon(lat,lng,rings[i]))return false;   return true; } function allAdminZones(){return [...PUBLISHED_SERVICE_ZONES,...manualServiceZones]} function manualZoneMatch(type,lat,lng){   const hits=allAdminZones().filter(z=>z.type===type&&pointInSimplePolygon(lat,lng,z.points||[]));return hits.length?hits[hits.length-1]:null; } async function inferredZoneMatchForAddress(type,ctx){   // 주소카드와 지도 ① 수거권역이 서로 다른 결과를 내지 않도록 실제 표시 폴리곤을 그대로 판정합니다.   return ensureServiceZoneMatch(type,ctx); } function renderServiceZoneSchedule(type,match,ctx,options={}){   if(!match)return;   const company=match.provider||routeCompany(match.vehicle);   const vehicles=(match.vehicles||[]).length?match.vehicles:[match.vehicle].filter(Boolean);   const plate=match.plate||((vehicles.length===1)?routePlate(vehicles[0]):'');   if(!options.fromMap)selectedVehicle[type]=company||match.vehicle||'전체';   const days=DAYS.filter(d=>(match.days||[]).includes(d));const badge=document.getElementById(type+'Badge');   const estimated=match.zoneStatus==='estimated'||!!match.estimated;   const estimateMark=estimated?' (추정)':'';   if(match.contractLayer)badge.textContent=`오창산업단지 대행${estimateMark} · ${days.join('·')}`;   else if(company)badge.textContent=`대행권역${estimateMark} · ${days.join('·')}`;   else if(match.riGrouped)badge.textContent=`리 단위${estimateMark} · ${days.join('·')}`;   else if

### buildService
Name,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized}; } async function ensureServiceZoneMatch(type,ctx){   let match=serviceZoneOverlayMatch(type,ctx);if(match)return match;   if(!serviceZoneBuilt[type]){     const hydrated=await hydrateCachedServiceZones(type);     if(hydrated){match=serviceZoneOverlayMatch(type,ctx);if(match)return match}     await buildServiceZones(type);     match=serviceZoneOverlayMatch(type,ctx);   }   return match; }  function geoPointKm(lat,lng){   return {x:+lng*88,y:+lat*111}; } function pointSegmentDistanceKm(lat,lng,a,b){   if(!a||!b)return Infinity;   const p=geoPointKm(lat,lng), p1=geoPointKm(a.lat,a.lng), p2=geoPointKm(b.lat,b.lng);   const vx=p2.x-p1.x, vy=p2.y-p1.y;   const wx=p.x-p1.x, wy=p.y-p1.y;   const vv=vx*vx+vy*vy;   let t=vv>0?(wx*vx+wy*vy)/vv:0;   t=Math.max(0,Math.min(1,t));   return Math.hypot(p.x-(p1.x+t*vx),p.y-(p1.y+t*vy)); } const MAX_ADDRESS_ROUTE_DISTANCE_KM=0.020; function routeMatchLimitKm(district){const d=canonicalDistrict(district);return RURAL_ROUTE_LAYER_DISTRICTS.has(d)?MAX_ADDRESS_ROUTE_DISTANCE_KM:(RURAL_INFERRED_ROUTE_DISTRICTS.has(d)?RURAL_ROUTE_MATCH_KM:MAX_ADDRESS_ROUTE_DISTANCE_KM)} // 오창·내수·북이 20m function routeItemDistanceKm(item,lat,lng){   // 주소 20m 직접매칭은 실제 표시 수거선만 사용합니다.   
=='general'||activeLayer==='recycle')syncDongRouteVisibility(activeLayer);    // IndexedDB와 외부 사전계산 묶음에도 없는 노선만 백그라운드에서 실제 계산합니다.   const missing=specs.filter(s=>!existingDongRouteOverlay(s));   await mapWithConcurrency(missing,3,async spec=>{     try{await buildDongRouteOverlay(spec)}catch(e){console.warn('동지역 수거노선 준비 실패',spec,e)}     if(activeLayer===spec.type&&serviceLayerState.route)syncDongRouteVisibility(spec.type);   });    // 생활/재활용 수거권역은 별도 구조로 유지합니다. 근거노선 사전계산 자체는 동지역에만 집중합니다.   await buildServiceZones('general');   await buildServiceZones('recycle');   if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer);   window.__CJWASTE_PRECOMPUTE_READY=true; }  function scheduleDongRoutePreload(){   // v65: 직영 동지역 + 제일환경/현대환경 청원구 전체 제공노선을 캐시 복원/누락분 준비합니다.   preloadAllDongRoutes().catch(e=>console.warn('수거노선 사전준비 실패',e)); }    function featureBounds(feature){   const rings=outerRingsOfFeature(feature);if(!rings.length)return null;   let minLng=Infinity,maxLng=-Infinity,minLat=Infinity,maxLat=-Infinity;   rings.forEach(r=>r.forEach(p=>{const lng=+p[0],lat=+p[1];if(!Number.isFinite(lng)||!Number.isFinite(lat))return;minLng=Math.min(minLng,lng);maxLng=Math.max(maxLng,lng);minLat=Math.min(minLat,lat);maxLat=Math.max(maxLat,lat)}));   return Number.isFinite(minLng)?{minLng,maxLng,minLat,maxLat}:null; } function routePatternKey(spec){return `${spec.vehicle}|${routeNorm(expandR
yncDongRouteVisibility(activeLayer);    // IndexedDB와 외부 사전계산 묶음에도 없는 노선만 백그라운드에서 실제 계산합니다.   const missing=specs.filter(s=>!existingDongRouteOverlay(s));   await mapWithConcurrency(missing,3,async spec=>{     try{await buildDongRouteOverlay(spec)}catch(e){console.warn('동지역 수거노선 준비 실패',spec,e)}     if(activeLayer===spec.type&&serviceLayerState.route)syncDongRouteVisibility(spec.type);   });    // 생활/재활용 수거권역은 별도 구조로 유지합니다. 근거노선 사전계산 자체는 동지역에만 집중합니다.   await buildServiceZones('general');   await buildServiceZones('recycle');   if(activeLayer==='general'||activeLayer==='recycle')setLayer(activeLayer);   window.__CJWASTE_PRECOMPUTE_READY=true; }  function scheduleDongRoutePreload(){   // v65: 직영 동지역 + 제일환경/현대환경 청원구 전체 제공노선을 캐시 복원/누락분 준비합니다.   preloadAllDongRoutes().catch(e=>console.warn('수거노선 사전준비 실패',e)); }    function featureBounds(feature){   const rings=outerRingsOfFeature(feature);if(!rings.length)return null;   let minLng=Infinity,maxLng=-Infinity,minLat=Infinity,maxLat=-Infinity;   rings.forEach(r=>r.forEach(p=>{const lng=+p[0],lat=+p[1];if(!Number.isFinite(lng)||!Number.isFinite(lat))return;minLng=Math.min(minLng,lng);maxLng=Math.max(maxLng,lng);minLat=Math.min(minLat,lat);maxLat=Math.max(maxLat,lat)}));   return Number.isFinite(minLng)?{minLng,maxLng,minLat,maxLat}:null; } function routePatternKey(spec){return `${spec.vehicle}|${routeNorm(expandRouteShorthand(spec.raw||''))}`} functi
       district:region.district,riName:region.riName||'',vehicle:rect.group.vehicle,provider:rect.group.provider||routeCompany(rect.group.vehicle),days:rect.group.days,groupKey:rect.group.key,       ambiguous:false,estimated:true,neighborRatio:+rect.neighborRatio||0,neighborSupport:+rect.neighborSupport||0,neighborTotal:+rect.neighborTotal||0,automatic:true,rural,gridMerged:true,estimateReason:'outside-confirmed-route'     });   } } function isContractRegion(region){return false} async function buildServiceZones(type){   if(serviceZoneBuilt[type]){syncServiceZoneVisibility(type);return}   if(serviceZoneBuildPromises.has(type))return serviceZoneBuildPromises.get(type);   const task=(async()=>{     // 저장된 자동권역이 있으면 네트워크/재계산 없이 즉시 복원합니다.     if(await hydrateCachedServiceZones(type))return;     if(!serviceRegionLoadComplete)return;      clearServiceZoneOverlays(type);     const regions=[...serviceZoneRegions];     let done=0;     for(const region of regions){       if(isUnconfirmedRi(type,region.district,region.riName||'')){done++;continue}       if(RI_LAYER_DISTRICTS.has(region.district)){         // v66: 내수·오창·북이는 법정리 경계 자체를 ① 수거권역으로 사용합니다.         // 같은 리 안을 100m 격자로 다시 쪼개지 않습니다.         await buildRuralRiServiceZone(type,region);       }else{         const groups=await serviceRouteGroups(type,region.district,region.riName||'');         const usable=groups.filter(g=>g.items.length);        
);for(const t of ['general','recycle'])(serviceZoneOverlays[t]||[]).forEach(z=>z.polygon?.setMap(null));   const seen=new Set();Object.values(zonePolygons).flat().forEach(item=>{if(!item?.polygon||seen.has(item.polygon))return;seen.add(item.polygon);item.polygon.setMap(null)});   if(layer==='food'){     zonePolygons.food.forEach(item=>item.polygon.setMap(map));zonePolygons.foodExclusion.forEach(item=>item.polygon.setMap(map));   }else{     syncBoundaryVisibility();     if(serviceLayerState.zone)buildServiceZones(layer).then(()=>syncServiceZoneVisibility(layer)).catch(e=>console.warn('수거권역 생성 실패',e));     if(serviceLayerState.route)ensureDongRoutesForSelection(layer);else hideDongRouteOverlays();   }   updateMapBadge();   if(service){     forceActiveServiceLayerVisibility(layer);     setTimeout(()=>{if(activeLayer===layer)forceActiveServiceLayerVisibility(layer)},120);     setTimeout(()=>{if(activeLayer===layer)forceActiveServiceLayerVisibility(layer)},700);   } } function updateMapBadge(custom){   const el=document.getElementById('mapBadge');const labels={general:'생활쓰레기',food:'음식물쓰레기',recycle:'재활용쓰레기'};   if(activeLayer==='food'){     el.innerHTML=`<strong>음식물쓰레기 수거·분류배출 레이어</strong><span>${custom||'수거 시행지역과 분류배출 제외지역을 면색으로 구분합니다.'}</span><div class="food-legend"><span class="legend-item"><i class="legend-dot legend-mwf"></i>시행지역 · 월·수·금</span><span class="legend-item"><i class="legend-dot
Options); document.getElementById('adminDraw').addEventListener('click',startAdminDrawing); document.getElementById('adminFinish').addEventListener('click',finishAdminDrawing); document.getElementById('adminCancel').addEventListener('click',clearAdminDraft); document.getElementById('adminRebuild').addEventListener('click',async()=>{for(const t of ['general','recycle']){clearServiceZoneOverlays(t);serviceZoneBuilt[t]=false;serviceZoneCacheHydrated[t]=false;await deleteCachedServiceZones(t)}await buildServiceZones(activeLayer);setLayer(activeLayer);updateAdminStatus('자동권역을 다시 계산해 저장했습니다.')}); document.getElementById('adminExport').addEventListener('click',exportAdminZones); document.getElementById('adminImport').addEventListener('click',()=>document.getElementById('adminImportFile').click()); document.getElementById('adminImportFile').addEventListener('change',e=>{if(e.target.files?.[0])importAdminZones(e.target.files[0]);e.target.value=''}); document.getElementById('adminClear').addEventListener('click',()=>{if(confirm('이 브라우저에 저장된 수동 수거권역을 모두 삭제할까요?')){manualServiceZones=[];saveManualZones();for(const t of ['general','recycle'])rebuildManualZoneOverlays(t);setLayer(activeLayer)}}); loadSdk(); </script> </body> </html>   

### inferred
ch(day=>{     for(const riKey of keys){       const hit=(DIRECT_RI_DAYS[type]?.[day]||[]).find(x=>x.key===riKey);       (hit?.vehicles||[]).forEach(v=>addProviderToSchedule(byDay,day,v));     }   });    // 2) 원본 작업구간을 다시 검사한다.   //    - 리가 직접 명시된 경우   //    - 숫자 마을표기(구성3리/원통1구 등)   //    - 실제 주소를 대조해 확정한 시설명/사업장/학교/마을명   //    만 추가한다. ‘○○읍 일원’만으로 모든 리에 일괄 확장하지 않는다.   for(const route of (DIRECT_ROUTES[type]||[])){     for(const [day,raw] of Object.entries(route.days||{})){       const regionHint=inferredRegionsFor(type,route.vehicle,day).includes(district);       const nraw=routeNorm(raw);       // 검증된 시설/학교/마을 주소는 기존 행정동 추정표가 빠져 있어도 실제 법정리로 반영한다.       const verifiedHit=verifiedRuleMatches(raw,district,riName,riCode).length>0;       if(!regionHint&&!verifiedHit)continue;       const directTextHit=regionHint&&normalizedRiTextCandidates(riName,riCode).concat(extraRiTextAliases(district,riName)).some(x=>x&&nraw.includes(x));       if(directTextHit||verifiedHit)addProviderToSchedule(byDay,day,route.vehicle);     }   }    // 3) 원본 코스표와 실제 주소로 별도 확정한 일정 보강   const confirmed=CONFIRMED_RI_SCHEDULE_OVERRIDES[type]?.[`${district}|${riName}`]||{};   Object.entries(confirmed).forEach(([day,providers])=>{     (providers||[]).forEach(provider=>addProviderToSchedule(byDay,day,provider));   });    // 4) 위탁구역은 함수 시작부에서 직영과 분리해 위탁업체 일정만 반환합니다.   // 5) 동일 일정 상속: 화하리는 내둔리와 생활·재활용 일정을 동일하게 적용합니다.   const i
7280">※ 오창산업단지 제일환경 대행구역(${riLabel})에 적용합니다.</span></div>`;   }else{     document.getElementById('recycleDetail').innerHTML=`<div class="verified"><strong>수거방식:</strong> 대행수거<br><strong>수거업체:</strong> ${OCHANG_SCIENCE_OPERATOR.name} · ${OCHANG_SCIENCE_OPERATOR.phone}<br><strong>단독주택·상가·50세대 미만 수거일:</strong> ${days.join(' · ')}<br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 자체 위탁계약에 따라 처리합니다.<br><span style="color:#6b7280">※ 오창산업단지 제일환경 대행구역(${riLabel})에 적용합니다.</span></div>`;   } }  const DIRECT_INFERRED_REGIONS={"general":{"3344":{"월":["오창읍"],"화":["오창읍"],"수":["오창읍"],"목":["북이면","오창읍"],"금":["오창읍"]},"3346":{"월":["내수읍","율량사천동"],"화":["오근장동","율량사천동"],"수":["율량사천동"],"목":["내수읍","오근장동","율량사천동"],"금":["오근장동","율량사천동"]},"6244":{"월":["율량사천동"],"화":["오근장동","율량사천동"],"수":["율량사천동"],"목":["오근장동","율량사천동"],"금":["율량사천동"]},"6224":{"월":["내수읍","오근장동","율량사천동"],"화":["오근장동","율량사천동"],"수":["내수읍","오근장동","율량사천동"],"목":["내수읍","오근장동","율량사천동"],"금":["오근장동","율량사천동"]},"6138":{"월":["내수읍"],"화":["내수읍","북이면"],"수":["내수읍","북이면"],"목":["내수읍","북이면"],"금":["내수읍","북이면"]},"6157":{"월":["내덕2동"],"화":["율량사천동","내덕2동"],"수":["내덕2동"],"목":["내덕2동"],"금":["율량사천동","내덕2동"]}},"recycle":{"6544":{"월":["내수읍"],"화":["내수읍","북이면"],"수":["내수읍","북이면"],"목":["내수읍","북이면"],"금":["내수읍","북이면"]},"0263":{"월":["오근장동","율량사천동"],"화":["율량사천동"],"수":["오근장동","율량사천동"],"목":["오근장동","율량사천동"],"금":["율량사천동"]},"0261":{"월":["율량사천동","내덕2동"],"화":["내덕2동"],"수":["율량사천동","내덕2동"],"목":["내덕2동"],"금"
,{"key":"오창읍|꽃화산리","vehicles":["0262"]},{"key":"오창읍|두릉리","vehicles":["0262"]},{"key":"오창읍|백현리","vehicles":["0262"]},{"key":"오창읍|성산리","vehicles":["0262"]},{"key":"오창읍|성재리","vehicles":["0262"]},{"key":"오창읍|용두리","vehicles":["0262"]},{"key":"오창읍|후기리","vehicles":["0262"]}]}};  const DIRECT_REGION_ADDITIONS={   // 주성로279번길은 주성동(행정동 오근장동)에 있으므로 정적 권역추론에서 빠졌던 오근장동을 보강합니다.   general:{'3346':{'월':['오근장동'],'수':['오근장동']}},   recycle:{'9090':{'월':['오근장동'],'수':['오근장동'],'목':['오근장동'],'금':['오근장동']}} }; function inferredRegionsFor(type,vehicle,day){   const base=DIRECT_INFERRED_REGIONS[type]?.[vehicle]?.[day]||[];   const extra=DIRECT_REGION_ADDITIONS[type]?.[vehicle]?.[day]||[];   return [...new Set([...base,...extra])]; } function directVehicleDaysForDistrict(type,district){   const out={};   for(const route of (DIRECT_ROUTES[type]||[])){     const days=[];     for(const day of Object.keys(route.days||{})){       if(inferredRegionsFor(type,route.vehicle,day).includes(district))days.push(day);     }     if(days.length)out[route.vehicle]=DAYS.filter(d=>days.includes(d));   }   return out; } function directRegionDays(type,district){   const set=new Set();   Object.values(directVehicleDaysForDistrict(type,district)).forEach(days=>days.forEach(d=>set.add(d)));   return DAYS.filter(d=>set.has(d)); } function vehicleList(type){   return [...new Set((DIRECT_ROUTES[type]||[]).map(r=>r.vehicle))]; } function pr
vehicles":["0262"]},{"key":"오창읍|백현리","vehicles":["0262"]},{"key":"오창읍|성산리","vehicles":["0262"]},{"key":"오창읍|성재리","vehicles":["0262"]},{"key":"오창읍|용두리","vehicles":["0262"]},{"key":"오창읍|후기리","vehicles":["0262"]}]}};  const DIRECT_REGION_ADDITIONS={   // 주성로279번길은 주성동(행정동 오근장동)에 있으므로 정적 권역추론에서 빠졌던 오근장동을 보강합니다.   general:{'3346':{'월':['오근장동'],'수':['오근장동']}},   recycle:{'9090':{'월':['오근장동'],'수':['오근장동'],'목':['오근장동'],'금':['오근장동']}} }; function inferredRegionsFor(type,vehicle,day){   const base=DIRECT_INFERRED_REGIONS[type]?.[vehicle]?.[day]||[];   const extra=DIRECT_REGION_ADDITIONS[type]?.[vehicle]?.[day]||[];   return [...new Set([...base,...extra])]; } function directVehicleDaysForDistrict(type,district){   const out={};   for(const route of (DIRECT_ROUTES[type]||[])){     const days=[];     for(const day of Object.keys(route.days||{})){       if(inferredRegionsFor(type,route.vehicle,day).includes(district))days.push(day);     }     if(days.length)out[route.vehicle]=DAYS.filter(d=>days.includes(d));   }   return out; } function directRegionDays(type,district){   const set=new Set();   Object.values(directVehicleDaysForDistrict(type,district)).forEach(days=>days.forEach(d=>set.add(d)));   return DAYS.filter(d=>set.has(d)); } function vehicleList(type){   return [...new Set((DIRECT_ROUTES[type]||[]).map(r=>r.vehicle))]; } function providerList(type){   const out=[...vehicleList(type)];   fo
   recycle:{'9090':{'월':['오근장동'],'수':['오근장동'],'목':['오근장동'],'금':['오근장동']}} }; function inferredRegionsFor(type,vehicle,day){   const base=DIRECT_INFERRED_REGIONS[type]?.[vehicle]?.[day]||[];   const extra=DIRECT_REGION_ADDITIONS[type]?.[vehicle]?.[day]||[];   return [...new Set([...base,...extra])]; } function directVehicleDaysForDistrict(type,district){   const out={};   for(const route of (DIRECT_ROUTES[type]||[])){     const days=[];     for(const day of Object.keys(route.days||{})){       if(inferredRegionsFor(type,route.vehicle,day).includes(district))days.push(day);     }     if(days.length)out[route.vehicle]=DAYS.filter(d=>days.includes(d));   }   return out; } function directRegionDays(type,district){   const set=new Set();   Object.values(directVehicleDaysForDistrict(type,district)).forEach(days=>days.forEach(d=>set.add(d)));   return DAYS.filter(d=>set.has(d)); } function vehicleList(type){   return [...new Set((DIRECT_ROUTES[type]||[]).map(r=>r.vehicle))]; } function providerList(type){   const out=[...vehicleList(type)];   for(const company of [...new Set((CONTRACT_ROUTES[type]||[]).map(r=>r.provider).filter(Boolean))])out.push(company);   return [...new Set(out)]; } function providerDisplay(key){   if(key==='제일환경'||key==='현대환경')return `${key}(대행)`;   const m=contractRouteMeta(key);return m?`${m.provider} ${m.plate}`:`${key}호 직영`; } function groupedScheduleForDistrict(type,d
ction providerDisplay(key){   if(key==='제일환경'||key==='현대환경')return `${key}(대행)`;   const m=contractRouteMeta(key);return m?`${m.provider} ${m.plate}`:`${key}호 직영`; } function groupedScheduleForDistrict(type,district){   const byDay={};   const add=(day,key)=>{     if(!byDay[day])byDay[day]=[];     if(!byDay[day].some(x=>x.key===key))byDay[day].push({key,label:providerDisplay(key)});   };   for(const route of (DIRECT_ROUTES[type]||[])){     for(const day of Object.keys(route.days||{})){       if(inferredRegionsFor(type,route.vehicle,day).includes(district))add(day,route.vehicle);     }   }   for(const route of contractorZoneRoutes(type,district))for(const day of Object.keys(route.days||{}))add(day,route.provider);   DAYS.forEach(day=>{if(byDay[day])byDay[day].sort((a,b)=>a.key.localeCompare(b.key,'ko'))});   return byDay; } function providerMatches(scheduleByDay,day,provider){   if(provider==='전체')return true;   if(day==='전체')return Object.values(scheduleByDay||{}).some(arr=>(arr||[]).some(x=>x.key===provider));   return (scheduleByDay?.[day]||[]).some(x=>x.key===provider); } function groupedDaySummary(type,day='전체',provider='전체'){   const out=[];   const districts=['내수읍','오창읍','북이면','우암동','내덕1동','내덕2동','오근장동','율량사천동'];   districts.forEach(district=>{     const schedule=groupedScheduleForDistrict(type,district);     const days=day==='전체'?DAYS.filter(d=>schedule[d]?.length):[day];     co
type==='general';   if(matches.length) selectedVehicle[type]=matches[0].vehicle;   const allDays=new Set();   matches.forEach(m=>m.days.forEach(d=>allDays.add(d)));   const orderedDays=DAYS.filter(d=>allDays.has(d));   const badge=document.getElementById(type+'Badge');   badge.textContent=`직영 · ${orderedDays.join('·')}`;   renderDays(type+'Days',orderedDays);   const rows=matches.map(m=>{     const terms=m.terms.slice(0,4).map(escapeHtml).join(', ');     const regs=[...new Set(m.days.flatMap(d=>inferredRegionsFor(type,m.vehicle,d)))];     const regText=regs.length?` · 권역 ${regs.map(escapeHtml).join('·')}`:'';     return `<strong>${m.vehicle}호:</strong> ${m.days.join(' · ')}${regText}${terms?` <span style="color:#6b7280">(${terms})</span>`:''}`;   }).join('<br>');   let secondary='';   const sec=applicableSecondaryDays(matches,ctx);   if(sec.length) secondary=`<br><strong>${isGeneral?'불연성':'대형폐기물'}:</strong> ${sec.join(' · ')}`;   const apartmentNotice=type==='recycle'     ? '<br><br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 수거에 대해 자체 위탁계약을 체결하여 처리합니다.'     : '';   const inheritNote=ctx?.scheduleInheritedFrom     ? `<br><span style="color:#6b7280">※ ${escapeHtml(ctx.originalLegalRi||'해당 리')}는 사용자 지정 기준에 따라 ${escapeHtml(ctx.scheduleInheritedFrom)}와 동일한 수거일정을 적용했습니다.</span>`     : '';   document.getElementById(type+'Detail').innerHTML=     `<div class="verified"><strong>수거방식:</strong> 청
ntLegalEmd='',currentLegalRi='',currentAddress='',currentLat=null,currentLng=null,currentFoodExclusion=null,activeLayer='general',activeDay='전체'; const selectedVehicle={general:'전체',recycle:'전체'}; let zoneTooltipOverlay=null; const zonePolygons={general:[],food:[],foodExclusion:[],recycle:[],districtOutline:[]};  // 직영 동지역 + 대행업체 원본 수거노선을 차량·요일별 참고 노선으로 표시합니다. const DONG_ROUTE_DISTRICTS=new Set(['우암동','내덕1동','내덕2동','오근장동','율량사천동']); // v67: 내수·북이는 표시된 수거노선에서 20m 이내만 자동 수거대상으로 인정합니다. const RURAL_INFERRED_ROUTE_DISTRICTS=new Set(['오창읍','내수읍','북이면']); const RURAL_ROUTE_LAYER_DISTRICTS=new Set(['오창읍','내수읍','북이면']); const RURAL_ROUTE_MATCH_KM=0.020; // ② 근거노선: 직영은 동지역 중심으로 유지하되, 대행업체가 제공한 청원구 원본 노선은 읍·동 구분 없이 실제 대행구간을 표시합니다. const VISUAL_DONG_ROUTE_DISTRICTS=new Set(['우암동','내덕1동','내덕2동','오근장동','율량사천동']); const CHEONGWON_ROUTE_DISTRICTS=new Set(['내수읍','오창읍','북이면','우암동','내덕1동','내덕2동','오근장동','율량사천동']); const districtFeatureMap=new Map(); const riFeatureMap=new Map(); // 읍·면 주소의 리 내부 세부 코스 판정용 const dongRouteOverlays={general:[],recycle:[]}; const dongRouteBuiltKeys=new Set(); const routePlaceSearchCache=new Map();  // 동지역 수거노선 영구 캐시. // 첫 방문에는 백그라운드에서 차량×요일 노선을 계산하고 IndexedDB에 저장합니다. // 이후 같은 브라우저에서는 저장된 도로망 좌표를 즉시 사용하므로 차량/요일 전환이 빠릅니다. const DONG_ROUTE_CACHE_DB='cjwaste-dong-route-cache'; const DONG_ROUTE_CACHE_STORE='routes'; const DONG_ROUTE_CACHE_VERSION='v76-layer-visibility-refresh'; con
량사천동']},     '0264':{월:['율량사천동','오근장동'],화:['율량사천동','오근장동'],수:['율량사천동','오근장동'],목:['율량사천동','오근장동'],금:['오근장동']}   } }; function routeDongScope(type,vehicle,day=''){   const cm=contractRouteMeta(vehicle);if(cm)return [canonicalDistrict(cm.district)];   const exact=day&&ROUTE_DONG_DAY_SCOPE[type]?.[vehicle]?.[day];   return [...(exact||ROUTE_DONG_SCOPE[type]?.[vehicle]||[])]; } function routeRuralScope(type,vehicle,day=''){   if(contractRouteMeta(vehicle))return [];   const src=day     ? [...(DIRECT_INFERRED_REGIONS?.[type]?.[vehicle]?.[day]||[])]     : [...(SERVICE_ZONE_ROUTE_SCOPE[type]?.[vehicle]||[])];   return [...new Set(src.map(canonicalDistrict).filter(d=>RURAL_ROUTE_LAYER_DISTRICTS.has(d)))]; } function routeAllowedDistricts(spec){   const src=Array.isArray(spec?.districts)&&spec.districts.length?spec.districts:routeDongScope(spec?.type,spec?.vehicle,spec?.day);   return [...new Set(src.map(canonicalDistrict).filter(Boolean))]; } function routeScopeSignature(spec){return routeAllowedDistricts(spec).slice().sort().join('|');} function serviceZoneRouteScope(type,vehicle){   const cm=contractRouteMeta(vehicle);if(cm)return cm.zoneEligible?[canonicalDistrict(cm.district)]:[];   return [...(SERVICE_ZONE_ROUTE_SCOPE[type]?.[vehicle]||[])]; }  function sourceRoadTermsForSpec(spec){   return [...(SOURCE_ROUTE_ROADS[spec?.type]?.[spec?.vehicle]?.[spec?.day]||[])]; } function sourceAddressRa
  });   if(!hits.length)return null;   hits.sort((a,b)=>(a.ambiguous?2:(a.estimated?1:0))-(b.ambiguous?2:(b.estimated?1:0)));   const z=hits[0];   const dongEstimated=DONG_ROUTE_DISTRICTS.has(district)&&!z.manual&&!z.routeBuffer;   return {vehicle:z.vehicle,vehicles:[...(z.vehicles||[])],provider:z.provider||routeCompany(z.vehicle),plate:z.plate||'',days:[...(z.days||[])],distanceKm:Infinity,matchBasis:'displayed-zone',zoneStatus:(z.estimated||dongEstimated)?'estimated':(z.ambiguous?'boundary':'inferred'),estimated:!!z.estimated||dongEstimated,neighborRatio:Number.isFinite(+z.neighborRatio)?+z.neighborRatio:0,neighborSupport:+z.neighborSupport||0,neighborTotal:+z.neighborTotal||0,matchedRoads:[],district,riName,riGrouped:!!z.riGrouped,contractLayer:!!z.contractLayer,routeBuffer:!!z.routeBuffer,noSchedule:!!z.noSchedule,groupOutline:!!z.groupOutline,gridMerged:!!z.gridMerged,polygonized:!!z.polygonized}; } async function ensureServiceZoneMatch(type,ctx){   let match=serviceZoneOverlayMatch(type,ctx);if(match)return match;   if(!serviceZoneBuilt[type]){     const hydrated=await hydrateCachedServiceZones(type);     if(hydrated){match=serviceZoneOverlayMatch(type,ctx);if(match)return match}     await buildServiceZones(type);     match=serviceZoneOverlayMatch(type,ctx);   }   return match; }  function geoPointKm(lat,lng){   return {x:+lng*88,y:+lat*111}; } function pointSegmentDistanceKm(la
ty;   const p=geoPointKm(lat,lng), p1=geoPointKm(a.lat,a.lng), p2=geoPointKm(b.lat,b.lng);   const vx=p2.x-p1.x, vy=p2.y-p1.y;   const wx=p.x-p1.x, wy=p.y-p1.y;   const vv=vx*vx+vy*vy;   let t=vv>0?(wx*vx+wy*vy)/vv:0;   t=Math.max(0,Math.min(1,t));   return Math.hypot(p.x-(p1.x+t*vx),p.y-(p1.y+t*vy)); } const MAX_ADDRESS_ROUTE_DISTANCE_KM=0.020; function routeMatchLimitKm(district){const d=canonicalDistrict(district);return RURAL_ROUTE_LAYER_DISTRICTS.has(d)?MAX_ADDRESS_ROUTE_DISTANCE_KM:(RURAL_INFERRED_ROUTE_DISTRICTS.has(d)?RURAL_ROUTE_MATCH_KM:MAX_ADDRESS_ROUTE_DISTANCE_KM)} // 오창·내수·북이 20m function routeItemDistanceKm(item,lat,lng){   // 주소 20m 직접매칭은 실제 표시 수거선만 사용합니다.   let best=Infinity;   for(const seg of (item?.segments||[])){     for(let i=1;i<seg.length;i++){       best=Math.min(best,pointSegmentDistanceKm(lat,lng,seg[i-1],seg[i]));     }   }   return best; } function serviceZoneEvidenceDistanceKm(item,lat,lng){   // ① 수거권역 추정에는 선으로 만들기 어려운 시설·마을명도 '숨은 근거점'으로 활용합니다.   // 이 점들은 지도에 원/마커로 표시하지 않으며, 주소 20m 직접매칭에도 사용하지 않습니다.   let best=routeItemDistanceKm(item,lat,lng);   for(const p of (item?.zoneEvidencePoints||[])){     if(!Number.isFinite(+p.lat)||!Number.isFinite(+p.lng))continue;     best=Math.min(best,routePointDistance({lat:+lat,lng:+lng},{lat:+p.lat,lng:+p.lng}));   }   return best; } function routeSpecsForDistrict(type,district){   return allDongRouteSpecs().filter(s=>s.
eHtml(company)}<br><strong>차량:</strong> ${escapeHtml(routePlate(match.vehicle))}`     : `<strong>수거방식:</strong> 청원구 직영<br><strong>${escapeHtml(match.vehicle)}호</strong>`;   document.getElementById(type+'Detail').innerHTML=     `<div class="verified">${ownerLine}: ${orderedDays.join(' · ')}<span style="color:#6b7280">${distanceText}${roadText}</span>`+     `${secondary}<br><span style="color:#6b7280">※ ${basisText} 주소점에서 실제 표시 수거선까지 ${limitM}m 이내인 경우에만 수거요일을 인정하며, 다른 차량의 인접노선 요일을 섞지 않습니다.${RURAL_INFERRED_ROUTE_DISTRICTS.has(district)?' 내수·북이는 원본 코스 순서를 따라 추정한 차량이동 도로구간도 수거노선에 포함합니다.':''}</span>${apartmentNotice}</div>`; } function restoredRuralRiSchedule(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   if(!RURAL_INFERRED_ROUTE_DISTRICTS.has(district))return null;   let ri=normalizeRi(ctx?.legalRi||'');   if(!ri)return null;   const originalRi=ri;   let inheritedFrom='';   // 사용자 지정 규칙: 북이면 광암리는 금암리 수거일정을 그대로 상속합니다.   if(district==='북이면'&&ri==='광암리'){ri='금암리';inheritedFrom='금암리'}   const key=`${district}|${ri}`;   const byDay={};   for(const day of DAYS){     const row=(DIRECT_RI_DAYS[type]?.[day]||[]).find(x=>x.key===key);     if(row?.vehicles?.length)byDay[day]=[...new Set(row.vehicles.map(String))];   }   const overrides=CONFIRMED_RI_SCHEDULE_OVERRIDES[type]?.[key]||{};   for(const [day,vehicles] of Object.entries(overrides)){     byDay[day]=[...new S
erLine}: ${orderedDays.join(' · ')}<span style="color:#6b7280">${distanceText}${roadText}</span>`+     `${secondary}<br><span style="color:#6b7280">※ ${basisText} 주소점에서 실제 표시 수거선까지 ${limitM}m 이내인 경우에만 수거요일을 인정하며, 다른 차량의 인접노선 요일을 섞지 않습니다.${RURAL_INFERRED_ROUTE_DISTRICTS.has(district)?' 내수·북이는 원본 코스 순서를 따라 추정한 차량이동 도로구간도 수거노선에 포함합니다.':''}</span>${apartmentNotice}</div>`; } function restoredRuralRiSchedule(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   if(!RURAL_INFERRED_ROUTE_DISTRICTS.has(district))return null;   let ri=normalizeRi(ctx?.legalRi||'');   if(!ri)return null;   const originalRi=ri;   let inheritedFrom='';   // 사용자 지정 규칙: 북이면 광암리는 금암리 수거일정을 그대로 상속합니다.   if(district==='북이면'&&ri==='광암리'){ri='금암리';inheritedFrom='금암리'}   const key=`${district}|${ri}`;   const byDay={};   for(const day of DAYS){     const row=(DIRECT_RI_DAYS[type]?.[day]||[]).find(x=>x.key===key);     if(row?.vehicles?.length)byDay[day]=[...new Set(row.vehicles.map(String))];   }   const overrides=CONFIRMED_RI_SCHEDULE_OVERRIDES[type]?.[key]||{};   for(const [day,vehicles] of Object.entries(overrides)){     byDay[day]=[...new Set([...(byDay[day]||[]),...(vehicles||[]).map(String)])];   }   const days=DAYS.filter(day=>byDay[day]?.length);   if(!days.length)return null;   return {district,ri,originalRi,inheritedFrom,key,days,byDay}; } function renderRestoredRuralRiSchedule(type,c
 대해 자체 위탁계약을 체결하여 처리합니다.'     : '';   document.getElementById(type+'Detail').innerHTML=     `<div class="pending"><strong>${escapeHtml(district)} 주소 위치 기준</strong><br>`+     `현재 자동 수거권역을 확정할 수 있는 근거노선이 부족해 이 주소의 ${type==='general'?'생활':'재활용'} 수거요일을 자동 지정하지 않습니다. 관리자 수거권역을 확인하거나 원본 코스를 추가 확인해주세요.${apartmentNotice}</div>`; } async function nearestRouteMatchesForAddress(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   if(!DONG_ROUTE_DISTRICTS.has(district)&&!RURAL_INFERRED_ROUTE_DISTRICTS.has(district))return null;   if(!Number.isFinite(+ctx.lat)||!Number.isFinite(+ctx.lng))return null;    const specs=routeSpecsForDistrict(type,district);   if(!specs.length)return [];    const items=await ensureDistrictRouteItems(type,district);   const itemMap=new Map(items.map(item=>[`${item.vehicle}|${item.day}`,item]));   const addressRoads=extractAddressRoadNames(ctx.address);    const maxRouteKm=routeMatchLimitKm(district);   const measures=specs.map(spec=>{     const item=itemMap.get(`${spec.vehicle}|${spec.day}`)||null;     const distanceKm=item?routeItemDistanceKm(item,+ctx.lat,+ctx.lng):Infinity;     const roads=matchedAddressRoads(spec,addressRoads);     return {spec,item,distanceKm,roads,roadHit:roads.length>0};   });    // 주소점에서 실제 표시 수거선까지 동지역 20m, 내수·북이 20m 이내인 노선만 후보로 인정합니다.   const eligible=measures.filter(m=>Number.isFinite(m.distanceKm)&&m.distanceKm<=m
dress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);       else renderNoNearbyRouteSchedule(type,district);       continue;     }     if(RURAL_INFERRED_ROUTE_DISTRICTS.has(district)){       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length)renderNearbyRouteSchedule(type,near,ctx);       else if(!renderRestoredRuralRiSchedule(type,ctx))renderNoNearbyRouteSchedule(type,district);       continue;     }     if(DONG_ROUTE_DISTRICTS.has(district)){       // 확실한 표시 수거노선 20m 이내가 최우선입니다. 그 밖의 빈 공간에서만 추정권역을 사용합니다.       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zone
ay(data.outsideRoadTerms)?data.outsideRoadTerms:[];   const addressRangeTerms=Array.isArray(data.addressRangeTerms)?data.addressRangeTerms:[];   const secondaryText=String(data.secondaryText||'').trim();   const secondaryKind=String(data.secondaryKind|| (spec.type==='general'?'불연성':'대형폐기물'));   const districtLabel=String(data.districtLabel||spec.districts.join('·'));   const roadMatchedCount=Number(data.roadMatchedCount)||0;   const roadRequestedCount=Number(data.roadRequestedCount)||0;   const inferredMovementCount=Number(data.inferredMovementCount)||0;   const distance=Number(data.distance)||0;   const fromCache=!!data.fromCache;   const color=DAY_COLORS[spec.day]||'#334155';    // ② 근거노선 레이어에서는 읍·면·동 모두 원본 코스에서 계산된 실제 도로구간을 표시합니다.   // 기본값은 OFF라서 권역 확인 화면은 복잡하지 않게 유지됩니다.   const displaySegments=segments;   const polylines=displaySegments.map(seg=>new kakao.maps.Polyline({     path:seg.map(p=>new kakao.maps.LatLng(+p.lat,+p.lng)),     strokeWeight:5,strokeColor:color,strokeOpacity:.9,strokeStyle:'solid'   }));   polylines.forEach(pl=>pl.setMap(null));    const circles=[];    const emphasize=()=>{     polylines.forEach(pl=>pl.setOptions({strokeWeight:7,strokeOpacity:.98}));   };   const normalize=()=>{     polylines.forEach(pl=>pl.setOptions({strokeWeight:5,strokeOpacity:.9}));   };   const basisText=()=>{     const api=roadRequestedCount?` · 도로망 ${roadMatchedCount}/${roadRequestedCou
sideRoadTerms:[];   const addressRangeTerms=Array.isArray(data.addressRangeTerms)?data.addressRangeTerms:[];   const secondaryText=String(data.secondaryText||'').trim();   const secondaryKind=String(data.secondaryKind|| (spec.type==='general'?'불연성':'대형폐기물'));   const districtLabel=String(data.districtLabel||spec.districts.join('·'));   const roadMatchedCount=Number(data.roadMatchedCount)||0;   const roadRequestedCount=Number(data.roadRequestedCount)||0;   const inferredMovementCount=Number(data.inferredMovementCount)||0;   const distance=Number(data.distance)||0;   const fromCache=!!data.fromCache;   const color=DAY_COLORS[spec.day]||'#334155';    // ② 근거노선 레이어에서는 읍·면·동 모두 원본 코스에서 계산된 실제 도로구간을 표시합니다.   // 기본값은 OFF라서 권역 확인 화면은 복잡하지 않게 유지됩니다.   const displaySegments=segments;   const polylines=displaySegments.map(seg=>new kakao.maps.Polyline({     path:seg.map(p=>new kakao.maps.LatLng(+p.lat,+p.lng)),     strokeWeight:5,strokeColor:color,strokeOpacity:.9,strokeStyle:'solid'   }));   polylines.forEach(pl=>pl.setMap(null));    const circles=[];    const emphasize=()=>{     polylines.forEach(pl=>pl.setOptions({strokeWeight:7,strokeOpacity:.98}));   };   const normalize=()=>{     polylines.forEach(pl=>pl.setOptions({strokeWeight:5,strokeOpacity:.9}));   };   const basisText=()=>{     const api=roadRequestedCount?` · 도로망 ${roadMatchedCount}/${roadRequestedCount}`:'';     const outside=outside
개`:'';     const blocked=outOfCheongwonTerms.length?` · 청원구 밖 차단 ${outOfCheongwonTerms.length}개`:'';     const roadHandled=coveredRoadTerms.length+outsideRoadTerms.length;     const roads=requiredRoadTerms.length?` · 원본 명시도로 ${roadHandled}/${requiredRoadTerms.length} (동선 ${coveredRoadTerms.length}·읍면 ${outsideRoadTerms.length})`:'';     const evidence=zoneEvidencePoints.length?` · 권역근거점 ${zoneEvidencePoints.length}개`:'';     return `원본 ${termsCount}개 항목 · 표시 수거구간 ${segments.length}개${evidence}${inferredMovementCount?` · 내수·북이 차량이동 추정 ${inferredMovementCount}구간`:''}${roads}${api}${outside}${blocked} · 미해결 ${missingTerms.length}개`;   };   const mouseover=e=>{     emphasize();     showZoneTooltip(e.latLng,`${spec.vehicle}호 · ${spec.day}요일`,`${districtLabel} 수거구간`,basisText());   };   const mousemove=e=>moveZoneTooltip(e.latLng);   const mouseout=()=>{normalize();hideZoneTooltip()};   const click=()=>{     const original=String(spec.raw||'').replace(/\s+/g,' ').trim();     const apiNote=roadRequestedCount?` · 실제 도로망 확인 ${roadMatchedCount}/${roadRequestedCount}항목`:'';     const outsideNote=outsideTerms.length?` · 읍·면/다른 권역 처리: ${escapeHtml(outsideTerms.join(', '))}`:'';     const blockedNote=outOfCheongwonTerms.length?` · 청원구 밖 검색결과 차단: ${escapeHtml(outOfCheongwonTerms.join(', '))}`:'';     const outsideRoadNote=outsideRoadTerms.length?` · 읍·면 면레이어 처리 도로: ${escapeHtml(outsideRoadTerms.join(
ms.length?` · 청원구 밖 차단 ${outOfCheongwonTerms.length}개`:'';     const roadHandled=coveredRoadTerms.length+outsideRoadTerms.length;     const roads=requiredRoadTerms.length?` · 원본 명시도로 ${roadHandled}/${requiredRoadTerms.length} (동선 ${coveredRoadTerms.length}·읍면 ${outsideRoadTerms.length})`:'';     const evidence=zoneEvidencePoints.length?` · 권역근거점 ${zoneEvidencePoints.length}개`:'';     return `원본 ${termsCount}개 항목 · 표시 수거구간 ${segments.length}개${evidence}${inferredMovementCount?` · 내수·북이 차량이동 추정 ${inferredMovementCount}구간`:''}${roads}${api}${outside}${blocked} · 미해결 ${missingTerms.length}개`;   };   const mouseover=e=>{     emphasize();     showZoneTooltip(e.latLng,`${spec.vehicle}호 · ${spec.day}요일`,`${districtLabel} 수거구간`,basisText());   };   const mousemove=e=>moveZoneTooltip(e.latLng);   const mouseout=()=>{normalize();hideZoneTooltip()};   const click=()=>{     const original=String(spec.raw||'').replace(/\s+/g,' ').trim();     const apiNote=roadRequestedCount?` · 실제 도로망 확인 ${roadMatchedCount}/${roadRequestedCount}항목`:'';     const outsideNote=outsideTerms.length?` · 읍·면/다른 권역 처리: ${escapeHtml(outsideTerms.join(', '))}`:'';     const blockedNote=outOfCheongwonTerms.length?` · 청원구 밖 검색결과 차단: ${escapeHtml(outOfCheongwonTerms.join(', '))}`:'';     const outsideRoadNote=outsideRoadTerms.length?` · 읍·면 면레이어 처리 도로: ${escapeHtml(outsideRoadTerms.join(', '))}`:'';     const addressRangeNote=ad
peHtml(missingRoadTerms.join(', '))}`:'';     const missingNote=missingTerms.length?` · 미해결 항목: ${escapeHtml(missingTerms.join(', '))}`:'';     const rangeNote=rangeTerms.length?` · 범위표현: ${escapeHtml(rangeTerms.join(', '))}`:'';     const secondaryNote=secondaryText?` · ${secondaryKind}: ${escapeHtml(secondaryText)} ※ ${secondaryKind}은 주 수거구간과 별도 작업정보입니다.`:'';     updateMapBadge(`${districtLabel} · ${escapeHtml(routeDisplayLabel(spec.vehicle))} · ${spec.day}요일 · 실제 수거구간 ${segments.length}개 · ${inferredMovementCount?`내수·북이 차량이동 추정 ${inferredMovementCount}구간 포함`:`구간 사이 이동선 제외`}${apiNote}${distance?` · 표시구간 약 ${(distance/1000).toFixed(1)}km`:''}${outsideNote}${blockedNote}${rangeNote}${outsideRoadNote}${addressRangeNote}${missingRoadNote}${missingNote}${secondaryNote} · 원본 주 수거코스: ${escapeHtml(original)}`);   };    polylines.forEach(shape=>{     kakao.maps.event.addListener(shape,'mouseover',mouseover);     kakao.maps.event.addListener(shape,'mousemove',mousemove);     kakao.maps.event.addListener(shape,'mouseout',mouseout);     kakao.maps.event.addListener(shape,'click',click);   });    const item={     ...spec,district:districtLabel,pointsCount,termsCount,serviceTermCount,     missingTerms,outsideTerms,outOfCheongwonTerms,rangeTerms,requiredRoadTerms,coveredRoadTerms,outsideRoadTerms,missingRoadTerms,addressRangeTerms,secondaryText,secondaryKind,     roadMatchedCount,roadRequestedCount
';     const missingNote=missingTerms.length?` · 미해결 항목: ${escapeHtml(missingTerms.join(', '))}`:'';     const rangeNote=rangeTerms.length?` · 범위표현: ${escapeHtml(rangeTerms.join(', '))}`:'';     const secondaryNote=secondaryText?` · ${secondaryKind}: ${escapeHtml(secondaryText)} ※ ${secondaryKind}은 주 수거구간과 별도 작업정보입니다.`:'';     updateMapBadge(`${districtLabel} · ${escapeHtml(routeDisplayLabel(spec.vehicle))} · ${spec.day}요일 · 실제 수거구간 ${segments.length}개 · ${inferredMovementCount?`내수·북이 차량이동 추정 ${inferredMovementCount}구간 포함`:`구간 사이 이동선 제외`}${apiNote}${distance?` · 표시구간 약 ${(distance/1000).toFixed(1)}km`:''}${outsideNote}${blockedNote}${rangeNote}${outsideRoadNote}${addressRangeNote}${missingRoadNote}${missingNote}${secondaryNote} · 원본 주 수거코스: ${escapeHtml(original)}`);   };    polylines.forEach(shape=>{     kakao.maps.event.addListener(shape,'mouseover',mouseover);     kakao.maps.event.addListener(shape,'mousemove',mousemove);     kakao.maps.event.addListener(shape,'mouseout',mouseout);     kakao.maps.event.addListener(shape,'click',click);   });    const item={     ...spec,district:districtLabel,pointsCount,termsCount,serviceTermCount,     missingTerms,outsideTerms,outOfCheongwonTerms,rangeTerms,requiredRoadTerms,coveredRoadTerms,outsideRoadTerms,missingRoadTerms,addressRangeTerms,secondaryText,secondaryKind,     roadMatchedCount,roadRequestedCount,inferredMovementCount,roadResult:{dist
ver);     kakao.maps.event.addListener(shape,'mousemove',mousemove);     kakao.maps.event.addListener(shape,'mouseout',mouseout);     kakao.maps.event.addListener(shape,'click',click);   });    const item={     ...spec,district:districtLabel,pointsCount,termsCount,serviceTermCount,     missingTerms,outsideTerms,outOfCheongwonTerms,rangeTerms,requiredRoadTerms,coveredRoadTerms,outsideRoadTerms,missingRoadTerms,addressRangeTerms,secondaryText,secondaryKind,     roadMatchedCount,roadRequestedCount,inferredMovementCount,roadResult:{distance},cached:fromCache,     segments:segments.map(seg=>seg.map(p=>({lat:+p.lat,lng:+p.lng}))),     servicePoints:[],     zoneEvidencePoints:zoneEvidencePoints.map(p=>({lat:+p.lat,lng:+p.lng,name:p.name||'',address:p.address||'',term:p.term||'',district:p.district||routePointAnyDistrict(p)||''})),     polylines,circles,polyline:polylines[0]||null   };   dongRouteOverlays[spec.type].push(item);   return item; }   function compactOrderedRuralAnchors(points,maxPts=12){   const clean=[];   for(const p of (points||[])){     if(!p||!Number.isFinite(+p.lat)||!Number.isFinite(+p.lng))continue;     const q={lat:+p.lat,lng:+p.lng,name:p.name||'',term:p.term||'',district:p.district||routePointAnyDistrict(p)||''};     if(clean.length&&routePointDistance(clean[clean.length-1],q)<0.05)continue;     clean.push(q);   }   if(clean.length<=maxPts)return clean;   const out=[]; 
nst q={lat:+p.lat,lng:+p.lng,name:p.name||'',term:p.term||'',district:p.district||routePointAnyDistrict(p)||''};     if(clean.length&&routePointDistance(clean[clean.length-1],q)<0.05)continue;     clean.push(q);   }   if(clean.length<=maxPts)return clean;   const out=[];   for(let i=0;i<maxPts;i++){     const idx=Math.round(i*(clean.length-1)/(maxPts-1));     if(!out.length||idx!==out[out.length-1]._idx)out.push({...clean[idx],_idx:idx});   }   return out.map(({_idx,...p})=>p); } async function inferredRuralMovementSegments(anchorMap){   const segments=[];let inferredMovementCount=0;   for(const district of RURAL_INFERRED_ROUTE_DISTRICTS){     const anchors=compactOrderedRuralAnchors(anchorMap?.[district]||[],12);     if(anchors.length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const 
ict:p.district||routePointAnyDistrict(p)||''};     if(clean.length&&routePointDistance(clean[clean.length-1],q)<0.05)continue;     clean.push(q);   }   if(clean.length<=maxPts)return clean;   const out=[];   for(let i=0;i<maxPts;i++){     const idx=Math.round(i*(clean.length-1)/(maxPts-1));     if(!out.length||idx!==out[out.length-1]._idx)out.push({...clean[idx],_idx:idx});   }   return out.map(({_idx,...p})=>p); } async function inferredRuralMovementSegments(anchorMap){   const segments=[];let inferredMovementCount=0;   for(const district of RURAL_INFERRED_ROUTE_DISTRICTS){     const anchors=compactOrderedRuralAnchors(anchorMap?.[district]||[],12);     if(anchors.length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const existing=existingDongRouteOverlay(spec);   if(existing)return exis
lean.length&&routePointDistance(clean[clean.length-1],q)<0.05)continue;     clean.push(q);   }   if(clean.length<=maxPts)return clean;   const out=[];   for(let i=0;i<maxPts;i++){     const idx=Math.round(i*(clean.length-1)/(maxPts-1));     if(!out.length||idx!==out[out.length-1]._idx)out.push({...clean[idx],_idx:idx});   }   return out.map(({_idx,...p})=>p); } async function inferredRuralMovementSegments(anchorMap){   const segments=[];let inferredMovementCount=0;   for(const district of RURAL_INFERRED_ROUTE_DISTRICTS){     const anchors=compactOrderedRuralAnchors(anchorMap?.[district]||[],12);     if(anchors.length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const existing=existingDongRouteOverlay(spec);   if(existing)return existing;   if(dongRouteBuildPromises.has(buildKey))return 
redRuralAnchors(anchorMap?.[district]||[],12);     if(anchors.length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const existing=existingDongRouteOverlay(spec);   if(existing)return existing;   if(dongRouteBuildPromises.has(buildKey))return dongRouteBuildPromises.get(buildKey);    const task=(async()=>{     const cached=await getCachedDongRoute(spec);     if(cached?.segments?.length){       return createDongRouteOverlay(spec,{...cached,fromCache:true});     }      const touchedDistricts=[];     const descriptors=applySourceRoadAudit(spec,routeDescriptorsForMap(spec.raw));     const entries=[];     const outOfCheongwonTerms=[];     const rangeTerms=[];     const outsideTerms=[];     const missingTerms=[];     const requiredRoadTerms=[...new Set([       ...sourceRoadTermsForSpec(spec).map
length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const existing=existingDongRouteOverlay(spec);   if(existing)return existing;   if(dongRouteBuildPromises.has(buildKey))return dongRouteBuildPromises.get(buildKey);    const task=(async()=>{     const cached=await getCachedDongRoute(spec);     if(cached?.segments?.length){       return createDongRouteOverlay(spec,{...cached,fromCache:true});     }      const touchedDistricts=[];     const descriptors=applySourceRoadAudit(spec,routeDescriptorsForMap(spec.raw));     const entries=[];     const outOfCheongwonTerms=[];     const rangeTerms=[];     const outsideTerms=[];     const missingTerms=[];     const requiredRoadTerms=[...new Set([       ...sourceRoadTermsForSpec(spec).map(routeSearchAlias),       ...descriptors.filter(d=>isRoadRoute
   if(roadTerm){             const rk=routeSearchAlias(term);             if(requiredRoadTerms.includes(rk)&&!outsideRoadTerms.includes(rk))outsideRoadTerms.push(rk);           }         }else if(!outOfCheongwonTerms.includes(term))missingTerms.push(term);       }       entries.push({term,desc,isRoad:roadTerm,candidates,outside,rejectedOutOfGu});     }      const segments=[];     const servicePoints=[];     const zoneEvidencePoints=[];     const ruralMovementAnchors=Object.fromEntries([...RURAL_INFERRED_ROUTE_DISTRICTS].map(d=>[d,[]]));     let anchorPointCount=0;     let serviceTermCount=0;     let roadRequestedCount=0;     let roadMatchedCount=0;      // 중요: 각 코스 항목을 독립적으로 처리합니다.     // 이전처럼 A→B→C를 하나의 길찾기로 연결하지 않으므로 "다음 수거구간으로 이동하는 길"은 지도에 그려지지 않습니다.     for(const entry of entries){       if(!entry.candidates.length)continue;        const base=bestLocalCandidate(entry);       // 내수·북이는 원본 항목 순서를 보존해 각 리/도로/시설 사이 차량 이동경로를 도로망으로 추정합니다.       for(const ruralDistrict of RURAL_INFERRED_ROUTE_DISTRICTS){         const localCandidates=entry.candidates.filter(p=>canonicalDistrict(p.district||routePointAnyDistrict(p))===ruralDistrict);         if(!localCandidates.length)continue;         const localBase=bestLocalCandidate({...entry,candidates:localCandidates});         if(localBase)ruralMovementAnchors[ruralDistrict].push({lat:+localBase.lat,lng:+localBase.lng,name:localBase.name||entry.term
[...RURAL_INFERRED_ROUTE_DISTRICTS].map(d=>[d,[]]));     let anchorPointCount=0;     let serviceTermCount=0;     let roadRequestedCount=0;     let roadMatchedCount=0;      // 중요: 각 코스 항목을 독립적으로 처리합니다.     // 이전처럼 A→B→C를 하나의 길찾기로 연결하지 않으므로 "다음 수거구간으로 이동하는 길"은 지도에 그려지지 않습니다.     for(const entry of entries){       if(!entry.candidates.length)continue;        const base=bestLocalCandidate(entry);       // 내수·북이는 원본 항목 순서를 보존해 각 리/도로/시설 사이 차량 이동경로를 도로망으로 추정합니다.       for(const ruralDistrict of RURAL_INFERRED_ROUTE_DISTRICTS){         const localCandidates=entry.candidates.filter(p=>canonicalDistrict(p.district||routePointAnyDistrict(p))===ruralDistrict);         if(!localCandidates.length)continue;         const localBase=bestLocalCandidate({...entry,candidates:localCandidates});         if(localBase)ruralMovementAnchors[ruralDistrict].push({lat:+localBase.lat,lng:+localBase.lng,name:localBase.name||entry.term,term:entry.term,district:ruralDistrict});       }       // 도로선이 만들어지지 않는 시설·마을명도 권역 분할에는 중요한 근거입니다.       // 단, 실제 지도에는 별도 원/마커를 그리지 않습니다.       if(base && !entry.isRoad){         zoneEvidencePoints.push({lat:+base.lat,lng:+base.lng,name:base.name||entry.term,address:base.address||'',term:entry.term,district:base.district||routePointAnyDistrict(base)||''});       }       const anchors=await serviceAnchorsForEntry(entry,spec);       let termShown=false;        if(anchors.length>=2){   
f(termShown){         serviceTermCount++;         if(entry.isRoad){           const rk=routeSearchAlias(entry.term);           if(!coveredRoadTerms.includes(rk))coveredRoadTerms.push(rk);         }         if(base?.district&&!touchedDistricts.includes(base.district))touchedDistricts.push(base.district);       }else if(!missingTerms.includes(entry.term)&&!outsideTerms.includes(entry.term)&&!outOfCheongwonTerms.includes(entry.term)){         missingTerms.push(entry.term);       }     }      const inferredMovement=await inferredRuralMovementSegments(ruralMovementAnchors);     if(inferredMovement.segments.length)segments.push(...inferredMovement.segments);     const inferredMovementCount=inferredMovement.inferredMovementCount;      const uniqueEvidence=uniqueRoutePoints(zoneEvidencePoints);     if(!segments.length&&!uniqueEvidence.length)return null;      const districtLabel=touchedDistricts.length?touchedDistricts.join('·'):spec.districts.join('·');     const routeObj=(DIRECT_ROUTES[spec.type]||[]).find(r=>r.vehicle===spec.vehicle);     const secondaryText=String(routeObj?.secondary?.[spec.day]||'').trim();     const secondaryKind=spec.type==='general'?'불연성':'대형폐기물';      const missingRoadTerms=requiredRoadTerms.filter(r=>!coveredRoadTerms.includes(r)&&!outsideRoadTerms.includes(r));      const data={       segments,       servicePoints:[],       zoneEvidencePoints:uniqueEvidence,       d

### 추정
<!doctype html> <html lang="ko"> <head> <meta charset="utf-8" /> <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" /> <title>청원구 환경위생과 | 청원구 쓰레기 수거일 안내 v27</title> <!-- v67: 내수읍·북이면은 원본 차량×요일 수거코스의 항목 순서를 따라 도로망 차량이동경로를 추정해 ② 근거노선에 포함하고, 주소는 표시 수거노선 20m 이내에서만 일정을 확정합니다. 오창·내수·북이는 리 단위로 시각 그룹을 유지하되 내수·북이 전체 리를 수거대상으로 보지 않습니다. 오창산업단지는 제일환경 대행구간으로 별도 표시하며, 대행업체 공동주택(아파트) 노선·주소일정은 전부 제외합니다. --> <style> :root{   --bg:#f6f7f9;--panel:#fff;--text:#111827;--muted:#6b7280;--line:#e5e7eb;   --general:#17745a;--food:#df6b42;--recycle:#315ea8;--warn:#9a6700; } *{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;font-family:Pretendard,"Noto Sans KR",-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--text);background:var(--bg)}button,input{font:inherit} #landing{   position:fixed;inset:0;z-index:30;display:flex;align-items:center;justify-content:flex-start;   padding:clamp(24px,5vw,76px);transition:.32s ease;overflow:hidden;   background:url("assets/landing-bg.jpg") 18% 12% / 125% auto no-repeat; } #landing::before{   content:"";position:absolute;inset:0;pointer-events:none;   background: 
 id="recycleDetail" class="detail"></div>       </article>     </div>     <div class="source-note">자료 기준: 사용자가 제공한 <strong>2026년 6월 청원구 직영 생활·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>   </aside>    <section class="map-wrap">     <div id="map" class="map"></div>     <div class="layer-control">       <button class="layer-btn active" data-layer="general">생활</button>       <button class="layer-btn" data-layer="recycle">재활용</button>       <button class="layer-btn" data-layer="food">음식물</button>     </div>     <div id="vehicleFilter" class="vehicle-filter"></div>     <div id="dayFilter" class="day-filter">      
·재활용 코스표</strong>, <strong>제일환경 6구역 수거노선표</strong>, <strong>현대환경 2026.08.11 수거노선표</strong> 및 2025~2026 대행구역별 수거지역 자료와 청원구 읍·면·동/법정리 경계. 현대환경 자료는 청원구에 해당하는 <strong>내덕1동만</strong> 반영하고 흥덕구 자료는 제외했습니다. 생활·재활용은 <strong>① 수거권역 → ② 근거 수거노선</strong>의 2단계로 확인합니다. <strong>수거권역은 원본 코스의 차량·요일별 도로선과 시설·마을의 숨은 근거점을 함께 지리적으로 배치한 뒤, 같은 리 또는 행정동 안에서 가장 가까운 근거를 기준으로 자동 분할한 추정 권역</strong>입니다. 실제 GPS 주행경로나 공식 확정 GIS 권역이 아니므로 경계 부근은 담당자 확인이 필요합니다. 주소 수거일정은 화면에 표시되는 ① 수거권역 폴리곤 포함 여부를 그대로 사용하며, 관리자 편집으로 직접 그린 권역은 자동 추정권역보다 우선합니다. 자동권역과 근거노선은 브라우저 영구 캐시에 저장해 다음 접속부터 즉시 복원합니다. 제일환경·현대환경 대행구간은 새 수거노선표의 주소·차량·요일을 우선하며, 북이면 광암리→금암리 일정 상속은 기존 지정 규칙을 유지합니다. 음식물은 기존 분류배출 시행/제외지역 기준을 유지합니다.<br>경계 출처: 청원구 읍·면·동 행정경계는 mapcn-kr 웹용 경계를 우선 사용하고 admdongkor ver20260701 WGS84 경계를 예비 경로로 사용합니다. 법정리 경계는 화면에는 표시하지 않고 내수읍·오창읍·북이면의 수거권역 분할 계산에만 내부적으로 사용합니다.</div>   </aside>    <section class="map-wrap">     <div id="map" class="map"></div>     <div class="layer-control">       <button class="layer-btn active" data-layer="general">생활</button>       <button class="layer-btn" data-layer="recycle">재활용</button>       <button class="layer-btn" data-layer="food">음식물</button>     </div>     <div id="vehicleFilter" class="vehicle-filter"></div>     <div id="dayFilter" class="day-filter">       <button class="active" data-day="전체">전체요일</button>       <button data-day="월">월</button>       <button data-day="화">화</button>       <button
leFilter" class="vehicle-filter"></div>     <div id="dayFilter" class="day-filter">       <button class="active" data-day="전체">전체요일</button>       <button data-day="월">월</button>       <button data-day="화">화</button>       <button data-day="수">수</button>       <button data-day="목">목</button>       <button data-day="금">금</button>     </div>     <div id="structureControl" class="structure-control">       <span class="structure-label">레이어</span>       <button class="active" data-structure="zone">① 추정권역</button>       <button class="active" data-structure="route">② 근거노선</button>     </div>     <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>     <div id="adminPanel" class="admin-panel">       <h4>수거권역 관리자 편집</h4>       <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>       <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>       <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" 
eControl" class="structure-control">       <span class="structure-label">레이어</span>       <button class="active" data-structure="zone">① 추정권역</button>       <button class="active" data-structure="route">② 근거노선</button>     </div>     <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>     <div id="adminPanel" class="admin-panel">       <h4>수거권역 관리자 편집</h4>       <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>       <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>       <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>       <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>       <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel"
an class="structure-label">레이어</span>       <button class="active" data-structure="zone">① 추정권역</button>       <button class="active" data-structure="route">② 근거노선</button>     </div>     <div class="map-tools"><button id="adminToggle" class="admin-toggle">관리자 편집</button><button id="zoomIn">＋</button><button id="zoomOut">－</button></div>     <div id="adminPanel" class="admin-panel">       <h4>수거권역 관리자 편집</h4>       <div class="admin-help">확정 수거노선 밖의 추정권역이 실제와 다를 때 지도에서 직접 권역을 그립니다. 직접 그린 권역은 자동 추정권역보다 우선합니다.</div>       <div class="admin-row"><label>종류</label><select id="adminType"><option value="general">생활쓰레기</option><option value="recycle">재활용</option></select><label>차량</label><select id="adminVehicle"></select></div>       <div class="admin-row"><label>요일</label><div id="adminDays" class="admin-days"><label><input type="checkbox" value="월">월</label><label><input type="checkbox" value="화">화</label><label><input type="checkbox" value="수">수</label><label><input type="checkbox" value="목">목</label><label><input type="checkbox" value="금">금</label></div></div>       <div class="admin-row"><label>메모</label><input id="adminNote" type="text" placeholder="예: 마산리 북쪽 구역" style="flex:1"></div>       <div class="admin-actions"><button id="adminDraw" class="primary">새 권역 그리기</button><button id="adminFinish">그리기 완료</button><button id="adminCancel">취소</button><button id="adminRebuild">자동권역 재표시
et hash=0;for(const ch of sig)hash=(hash*31+ch.charCodeAt(0))>>>0;   return SCHEDULE_FALLBACK_COLORS[hash%SCHEDULE_FALLBACK_COLORS.length]; } function addProviderToSchedule(byDay,day,key){   if(!byDay[day])byDay[day]=[];   if(!byDay[day].some(x=>x.key===key))byDay[day].push({key,label:providerDisplay(key)}); } // 코스표의 시설명·마을명·도로명 주소를 실제 지번/법정리와 대조한 보강 규칙. // 리 이름이 직접 적히지 않은 작업구간을 법정리 레이어에 반영하기 위한 기준이며, // 긴 간선도로처럼 여러 리를 통과해 범위가 불명확한 표현은 여기서 임의 지정하지 않습니다. // 원본 코스표 + 실제 주소 확인으로 확정한 법정리 일정 보강. // 추정이 아니라 자료상 명시 또는 실제 작업지점 주소가 확인된 경우만 넣습니다. const CONFIRMED_RI_SCHEDULE_OVERRIDES={   general:{     '내수읍|초정리':{'월':['6138'],'화':['6138']}   },   recycle:{     '내수읍|초정리':{'월':['6544'],'화':['6544']}   } }; const VERIFIED_ROUTE_RI_RULES=[   // 오창읍   {district:'오창읍',ri:'창리',terms:['오창마트','오창중']},   {district:'오창읍',ri:'장대리',terms:['오창초','오창읍사무소','오창읍행정복지센터']},   {district:'오창읍',ri:'괴정리',terms:['농업기술원','충북농업기술원']},   {district:'오창읍',ri:'신평리',terms:['지게바위']},   {district:'오창읍',ri:'성산리',terms:['한우마실','미래지한옥마을','미래지전원마을','자모원','벨루아캠핑장','벨루아']},   {district:'오창읍',ri:'양청리',terms:['장미공원']},   {district:'오창읍',ri:'송대리',terms:['하나로마트','오창농협하나로마트']},   {district:'오창읍',ri:'구룡리',terms:['자연어린이집']},   {district:'오창읍',ris:['성재리','두릉리'],terms:['3산업단지','오창제3산업단지']},   // 오창에는 한글 이름이 같은 화산리가 2곳 있어 법정리 코드로 구분   {district:'오창읍',ri:'화산리',riCodes:['4311425322'],display:'빛화산리',terms:['빛화산','체리부로']},   {district:'오창읍',
icles||[]).forEach(v=>addProviderToSchedule(byDay,day,v));     }   });    // 2) 원본 작업구간을 다시 검사한다.   //    - 리가 직접 명시된 경우   //    - 숫자 마을표기(구성3리/원통1구 등)   //    - 실제 주소를 대조해 확정한 시설명/사업장/학교/마을명   //    만 추가한다. ‘○○읍 일원’만으로 모든 리에 일괄 확장하지 않는다.   for(const route of (DIRECT_ROUTES[type]||[])){     for(const [day,raw] of Object.entries(route.days||{})){       const regionHint=inferredRegionsFor(type,route.vehicle,day).includes(district);       const nraw=routeNorm(raw);       // 검증된 시설/학교/마을 주소는 기존 행정동 추정표가 빠져 있어도 실제 법정리로 반영한다.       const verifiedHit=verifiedRuleMatches(raw,district,riName,riCode).length>0;       if(!regionHint&&!verifiedHit)continue;       const directTextHit=regionHint&&normalizedRiTextCandidates(riName,riCode).concat(extraRiTextAliases(district,riName)).some(x=>x&&nraw.includes(x));       if(directTextHit||verifiedHit)addProviderToSchedule(byDay,day,route.vehicle);     }   }    // 3) 원본 코스표와 실제 주소로 별도 확정한 일정 보강   const confirmed=CONFIRMED_RI_SCHEDULE_OVERRIDES[type]?.[`${district}|${riName}`]||{};   Object.entries(confirmed).forEach(([day,providers])=>{     (providers||[]).forEach(provider=>addProviderToSchedule(byDay,day,provider));   });    // 4) 위탁구역은 함수 시작부에서 직영과 분리해 위탁업체 일정만 반환합니다.   // 5) 동일 일정 상속: 화하리는 내둔리와 생활·재활용 일정을 동일하게 적용합니다.   const inheritSource=RI_SCHEDULE_INHERIT[`${district}|${riName}`];   if(inheritSource){     const inherited=riScheduleFromSources(t
 판정은 상세 주소로 다시 확인해 주세요.':'';     document.getElementById('foodDetail').innerHTML=`<div class="verified"><strong>단독주택 배출:</strong> ${food.days.join(' · ')}<br><strong>수거:</strong> ${food.pickup}<br><strong>수거업체:</strong> ${food.operator}${food.phone?' · '+food.phone:''}<br><strong>공동주택:</strong> 상시배출${extra}</div>`;   }else setPending('food','해당 지역의 음식물 일정을 공식자료에서 확인하지 못했습니다.');    const ctx={address:currentAddress,district,legalEmd,legalRi,lat:currentLat,lng:currentLng};    // 생활쓰레기: 확정되지 않은 리는 추정하지 않고 미확인으로 처리합니다.   if(isUnconfirmedRi('general',legalEmd||district,legalRi)){     selectedVehicle.general='전체';     document.getElementById('generalBadge').textContent='수거정보 미확인';     renderDays('generalDays',[]);     document.getElementById('generalDetail').innerHTML=`<div class="pending"><strong>${escapeHtml([legalEmd||district,legalRi].filter(Boolean).join(' '))}</strong><br>현재 확정된 수거자료가 없어 지도 레이어에서도 제외한 지역입니다. 추후 확인자료 반영 전까지 수거요일을 안내하지 않습니다.</div>`;   }else{     const directGeneralCtx=inheritedScheduleContext(ctx);     const inheritedGeneral=!!directGeneralCtx.scheduleInheritedFrom;     const scienceGeneral=false;     if(false){     }else if(RI_LAYER_DISTRICTS.has(canonicalDistrict(legalEmd||district)) && !inheritedGeneral){       selectedVehicle.general='전체';       if(!renderRestoredRuralRiSchedule('general',ctx)){         document.getElementById('generalBadge').textContent='수거노선 
체';       if(directGeneral.length){         renderDirectSchedule('general',directGeneral,directGeneralCtx);       }else if(renderRestoredRuralRiSchedule('general',ctx)){       }else if(districtHasDirectCoverage('general',district)){         document.getElementById('generalBadge').textContent='직영 구간 확인';         renderDays('generalDays',[]);         document.getElementById('generalDetail').innerHTML=`<div class="pending"><strong>청원구 직영 수거구간이 포함된 지역입니다.</strong><br>현재 검색주소와 코스표가 명확히 일치하지 않아 수거요일을 추정하지 않습니다.</div>`;       }else{         const generals=findRegionEntries('general',district);         if(generals.length){           document.getElementById('generalBadge').textContent='요일 확인 필요';renderDays('generalDays',[]);           const rows=generals.map(x=>`<strong>${x.housing==='apartment'?'공동주택':'단독주택'} 대행:</strong> ${x.operator}${x.phone?' · '+x.phone:''}`).join('<br>');           document.getElementById('generalDetail').innerHTML=`<div class="pending">${rows}<br><strong>배출시간:</strong> 일몰 후부터 자정 전<br>제공된 노선표에서 이 주소와 일치하는 세부 코스를 확인하지 못했습니다.</div>`;         }else setPending('general','현재 확정된 자료만으로는 이 지역의 생활쓰레기 수거정보를 안내할 수 없습니다.');       }     }   }    // 재활용: 확정되지 않은 리는 추정하지 않고 미확인으로 처리합니다.   if(isUnconfirmedRi('recycle',legalEmd||district,legalRi)){     selectedVehicle.recycle='전체';     document.getElementById('recycleBadge').textContent='수거정보 미확인';     renderDays('recycleDays',[])
요일 확인 필요';renderDays('generalDays',[]);           const rows=generals.map(x=>`<strong>${x.housing==='apartment'?'공동주택':'단독주택'} 대행:</strong> ${x.operator}${x.phone?' · '+x.phone:''}`).join('<br>');           document.getElementById('generalDetail').innerHTML=`<div class="pending">${rows}<br><strong>배출시간:</strong> 일몰 후부터 자정 전<br>제공된 노선표에서 이 주소와 일치하는 세부 코스를 확인하지 못했습니다.</div>`;         }else setPending('general','현재 확정된 자료만으로는 이 지역의 생활쓰레기 수거정보를 안내할 수 없습니다.');       }     }   }    // 재활용: 확정되지 않은 리는 추정하지 않고 미확인으로 처리합니다.   if(isUnconfirmedRi('recycle',legalEmd||district,legalRi)){     selectedVehicle.recycle='전체';     document.getElementById('recycleBadge').textContent='수거정보 미확인';     renderDays('recycleDays',[]);     document.getElementById('recycleDetail').innerHTML=`<div class="pending"><strong>${escapeHtml([legalEmd||district,legalRi].filter(Boolean).join(' '))}</strong><br>현재 확정된 수거자료가 없어 지도 레이어에서도 제외한 지역입니다. 추후 확인자료 반영 전까지 수거요일을 안내하지 않습니다.<br><br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 수거에 대해 자체 위탁계약을 체결하여 처리합니다.</div>`;   }else{     const directRecycleCtx=inheritedScheduleContext(ctx);     const inheritedRecycle=!!directRecycleCtx.scheduleInheritedFrom;     const scienceRecycle=false;     if(false){     }else if(RI_LAYER_DISTRICTS.has(canonicalDistrict(legalEmd||district)) && !inheritedRecycle){       selectedVehicle.recycle='전체';       if(!renderRestoredRuralRiSchedule('rec
      if(directRecycle.length){         renderDirectSchedule('recycle',directRecycle,directRecycleCtx);       }else if(renderRestoredRuralRiSchedule('recycle',ctx)){       }else if(districtHasDirectCoverage('recycle',district)){         document.getElementById('recycleBadge').textContent='직영 구간 확인';         renderDays('recycleDays',[]);         document.getElementById('recycleDetail').innerHTML=`<div class="pending"><strong>청원구 직영 재활용 수거구간이 포함된 지역입니다.</strong><br>현재 검색주소와 코스표가 명확히 일치하지 않아 수거요일을 추정하지 않습니다.<br><br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 수거에 대해 자체 위탁계약을 체결하여 처리합니다.</div>`;       }else{         const recycles=findRegionEntries('recycle',district);         if(recycles.length){           document.getElementById('recycleBadge').textContent='요일 확인 필요';renderDays('recycleDays',[]);           const rows=recycles.map(x=>`<strong>${x.housing==='apartment'?'공동주택':'단독주택'} 대행:</strong> ${x.operator}${x.phone?' · '+x.phone:''}`).join('<br>');           document.getElementById('recycleDetail').innerHTML=`<div class="pending">${rows}<br>제공된 노선표에서 이 주소와 일치하는 세부 코스를 확인하지 못했습니다.<br><br><strong>공동주택 안내:</strong> 의무관리대상 공동주택은 재활용품 수거에 대해 자체 위탁계약을 체결하여 처리합니다.</div>`;         }else{           setPending('recycle','현재 확정된 자료만으로는 이 지역의 재활용쓰레기 수거정보를 안내할 수 없습니다.');           document.getElementById('recycleDetail').innerHTML += `<div class="pending" style="margin-top:8px"><strong>공동주택 안내
ISTRICTS.has(d)?MAX_ADDRESS_ROUTE_DISTANCE_KM:(RURAL_INFERRED_ROUTE_DISTRICTS.has(d)?RURAL_ROUTE_MATCH_KM:MAX_ADDRESS_ROUTE_DISTANCE_KM)} // 오창·내수·북이 20m function routeItemDistanceKm(item,lat,lng){   // 주소 20m 직접매칭은 실제 표시 수거선만 사용합니다.   let best=Infinity;   for(const seg of (item?.segments||[])){     for(let i=1;i<seg.length;i++){       best=Math.min(best,pointSegmentDistanceKm(lat,lng,seg[i-1],seg[i]));     }   }   return best; } function serviceZoneEvidenceDistanceKm(item,lat,lng){   // ① 수거권역 추정에는 선으로 만들기 어려운 시설·마을명도 '숨은 근거점'으로 활용합니다.   // 이 점들은 지도에 원/마커로 표시하지 않으며, 주소 20m 직접매칭에도 사용하지 않습니다.   let best=routeItemDistanceKm(item,lat,lng);   for(const p of (item?.zoneEvidencePoints||[])){     if(!Number.isFinite(+p.lat)||!Number.isFinite(+p.lng))continue;     best=Math.min(best,routePointDistance({lat:+lat,lng:+lng},{lat:+p.lat,lng:+p.lng}));   }   return best; } function routeSpecsForDistrict(type,district){   return allDongRouteSpecs().filter(s=>s.type===type && (s.districts||[]).includes(district)); } async function mapWithConcurrency(items,limit,worker){   const out=new Array(items.length);   let cursor=0;   async function run(){     while(true){       const i=cursor++;       if(i>=items.length)return;       try{out[i]=await worker(items[i],i)}       catch(e){console.warn('인접 수거노선 준비 실패',items[i],e);out[i]=null}     }   }   await Promise.all(Array.from({length:Math.min(limit,ite
te(match.vehicle))}`     : `<strong>수거방식:</strong> 청원구 직영<br><strong>${escapeHtml(match.vehicle)}호</strong>`;   document.getElementById(type+'Detail').innerHTML=     `<div class="verified">${ownerLine}: ${orderedDays.join(' · ')}<span style="color:#6b7280">${distanceText}${roadText}</span>`+     `${secondary}<br><span style="color:#6b7280">※ ${basisText} 주소점에서 실제 표시 수거선까지 ${limitM}m 이내인 경우에만 수거요일을 인정하며, 다른 차량의 인접노선 요일을 섞지 않습니다.${RURAL_INFERRED_ROUTE_DISTRICTS.has(district)?' 내수·북이는 원본 코스 순서를 따라 추정한 차량이동 도로구간도 수거노선에 포함합니다.':''}</span>${apartmentNotice}</div>`; } function restoredRuralRiSchedule(type,ctx){   const district=canonicalDistrict(ctx?.legalEmd||ctx?.district||'');   if(!RURAL_INFERRED_ROUTE_DISTRICTS.has(district))return null;   let ri=normalizeRi(ctx?.legalRi||'');   if(!ri)return null;   const originalRi=ri;   let inheritedFrom='';   // 사용자 지정 규칙: 북이면 광암리는 금암리 수거일정을 그대로 상속합니다.   if(district==='북이면'&&ri==='광암리'){ri='금암리';inheritedFrom='금암리'}   const key=`${district}|${ri}`;   const byDay={};   for(const day of DAYS){     const row=(DIRECT_RI_DAYS[type]?.[day]||[]).find(x=>x.key===key);     if(row?.vehicles?.length)byDay[day]=[...new Set(row.vehicles.map(String))];   }   const overrides=CONFIRMED_RI_SCHEDULE_OVERRIDES[type]?.[key]||{};   for(const [day,vehicles] of Object.entries(overrides)){     byDay[day]=[...new Set([...(byDay[day]||[]),...(vehicles||[]).map(String)])
       continue;     }     if(RURAL_INFERRED_ROUTE_DISTRICTS.has(district)){       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length)renderNearbyRouteSchedule(type,near,ctx);       else if(!renderRestoredRuralRiSchedule(type,ctx))renderNoNearbyRouteSchedule(type,district);       continue;     }     if(DONG_ROUTE_DISTRICTS.has(district)){       // 확실한 표시 수거노선 20m 이내가 최우선입니다. 그 밖의 빈 공간에서만 추정권역을 사용합니다.       const near=await nearestRouteMatchesForAddress(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(near?.length){renderNearbyRouteSchedule(type,near,ctx);continue}       const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});       if(seq!==nearbyScheduleRequestSeq)return;       if(match)renderServiceZoneSchedule(type,{...match,estimated:true,zoneStatus:'estimated'},ctx);       else renderNoNearbyRouteSchedule(type,district);       continue;     }     // 오창읍은 기존 리 단위/대행 확정규칙을 유지합니다.     const match=await ensureServiceZoneMatch(type,{...ctx,district,legalEmd:district});     if(seq!==nearbyScheduleRequestSeq)return;     if(match)renderServiceZoneSchedule(type,match,ctx);     else renderNoNearbyRouteSchedule(type,district);   }   if(seq!==nearbyScheduleRequestSeq)return;   renderVehicleFilter();   i
onTerms.length?` · 청원구 밖 차단 ${outOfCheongwonTerms.length}개`:'';     const roadHandled=coveredRoadTerms.length+outsideRoadTerms.length;     const roads=requiredRoadTerms.length?` · 원본 명시도로 ${roadHandled}/${requiredRoadTerms.length} (동선 ${coveredRoadTerms.length}·읍면 ${outsideRoadTerms.length})`:'';     const evidence=zoneEvidencePoints.length?` · 권역근거점 ${zoneEvidencePoints.length}개`:'';     return `원본 ${termsCount}개 항목 · 표시 수거구간 ${segments.length}개${evidence}${inferredMovementCount?` · 내수·북이 차량이동 추정 ${inferredMovementCount}구간`:''}${roads}${api}${outside}${blocked} · 미해결 ${missingTerms.length}개`;   };   const mouseover=e=>{     emphasize();     showZoneTooltip(e.latLng,`${spec.vehicle}호 · ${spec.day}요일`,`${districtLabel} 수거구간`,basisText());   };   const mousemove=e=>moveZoneTooltip(e.latLng);   const mouseout=()=>{normalize();hideZoneTooltip()};   const click=()=>{     const original=String(spec.raw||'').replace(/\s+/g,' ').trim();     const apiNote=roadRequestedCount?` · 실제 도로망 확인 ${roadMatchedCount}/${roadRequestedCount}항목`:'';     const outsideNote=outsideTerms.length?` · 읍·면/다른 권역 처리: ${escapeHtml(outsideTerms.join(', '))}`:'';     const blockedNote=outOfCheongwonTerms.length?` · 청원구 밖 검색결과 차단: ${escapeHtml(outOfCheongwonTerms.join(', '))}`:'';     const outsideRoadNote=outsideRoadTerms.length?` · 읍·면 면레이어 처리 도로: ${escapeHtml(outsideRoadTerms.join(', '))}`:'';     const addressR
)}`:'';     const missingNote=missingTerms.length?` · 미해결 항목: ${escapeHtml(missingTerms.join(', '))}`:'';     const rangeNote=rangeTerms.length?` · 범위표현: ${escapeHtml(rangeTerms.join(', '))}`:'';     const secondaryNote=secondaryText?` · ${secondaryKind}: ${escapeHtml(secondaryText)} ※ ${secondaryKind}은 주 수거구간과 별도 작업정보입니다.`:'';     updateMapBadge(`${districtLabel} · ${escapeHtml(routeDisplayLabel(spec.vehicle))} · ${spec.day}요일 · 실제 수거구간 ${segments.length}개 · ${inferredMovementCount?`내수·북이 차량이동 추정 ${inferredMovementCount}구간 포함`:`구간 사이 이동선 제외`}${apiNote}${distance?` · 표시구간 약 ${(distance/1000).toFixed(1)}km`:''}${outsideNote}${blockedNote}${rangeNote}${outsideRoadNote}${addressRangeNote}${missingRoadNote}${missingNote}${secondaryNote} · 원본 주 수거코스: ${escapeHtml(original)}`);   };    polylines.forEach(shape=>{     kakao.maps.event.addListener(shape,'mouseover',mouseover);     kakao.maps.event.addListener(shape,'mousemove',mousemove);     kakao.maps.event.addListener(shape,'mouseout',mouseout);     kakao.maps.event.addListener(shape,'click',click);   });    const item={     ...spec,district:districtLabel,pointsCount,termsCount,serviceTermCount,     missingTerms,outsideTerms,outOfCheongwonTerms,rangeTerms,requiredRoadTerms,coveredRoadTerms,outsideRoadTerms,missingRoadTerms,addressRangeTerms,secondaryText,secondaryKind,     roadMatchedCount,roadRequestedCount,inferredMovementCount,roadR
nst idx=Math.round(i*(clean.length-1)/(maxPts-1));     if(!out.length||idx!==out[out.length-1]._idx)out.push({...clean[idx],_idx:idx});   }   return out.map(({_idx,...p})=>p); } async function inferredRuralMovementSegments(anchorMap){   const segments=[];let inferredMovementCount=0;   for(const district of RURAL_INFERRED_ROUTE_DISTRICTS){     const anchors=compactOrderedRuralAnchors(anchorMap?.[district]||[],12);     if(anchors.length<2)continue;     // 원본 코스의 항목 순서를 유지해 도로망 길찾기로 항목 사이 차량 이동경로를 추정합니다.     // 길찾기가 실패하면 직선을 억지로 그리지 않고 해당 이동구간은 생략합니다.     const roadResult=await fetchRoadFollowingPath(anchors);     for(const roadPath of (roadResult?.segments||[])){       const local=clipRoutePathToDistricts(roadPath,[district]);       for(const seg of local){         if(seg.length<2)continue;         segments.push(seg.map(p=>({lat:+p.lat,lng:+p.lng})));         inferredMovementCount++;       }     }   }   return {segments,inferredMovementCount}; }  async function buildDongRouteOverlay(spec){   const buildKey=`${spec.type}|${spec.vehicle}|${spec.day}|${routeScopeSignature(spec)}`;   const existing=existingDongRouteOverlay(spec);   if(existing)return existing;   if(dongRouteBuildPromises.has(buildKey))return dongRouteBuildPromises.get(buildKey);    const task=(async()=>{     const cached=await getCachedDongRoute(spec);     if(cached?.segments?.length){       return createDongRouteOverl
const ruralMovementAnchors=Object.fromEntries([...RURAL_INFERRED_ROUTE_DISTRICTS].map(d=>[d,[]]));     let anchorPointCount=0;     let serviceTermCount=0;     let roadRequestedCount=0;     let roadMatchedCount=0;      // 중요: 각 코스 항목을 독립적으로 처리합니다.     // 이전처럼 A→B→C를 하나의 길찾기로 연결하지 않으므로 "다음 수거구간으로 이동하는 길"은 지도에 그려지지 않습니다.     for(const entry of entries){       if(!entry.candidates.length)continue;        const base=bestLocalCandidate(entry);       // 내수·북이는 원본 항목 순서를 보존해 각 리/도로/시설 사이 차량 이동경로를 도로망으로 추정합니다.       for(const ruralDistrict of RURAL_INFERRED_ROUTE_DISTRICTS){         const localCandidates=entry.candidates.filter(p=>canonicalDistrict(p.district||routePointAnyDistrict(p))===ruralDistrict);         if(!localCandidates.length)continue;         const localBase=bestLocalCandidate({...entry,candidates:localCandidates});         if(localBase)ruralMovementAnchors[ruralDistrict].push({lat:+localBase.lat,lng:+localBase.lng,name:localBase.name||entry.term,term:entry.term,district:ruralDistrict});       }       // 도로선이 만들어지지 않는 시설·마을명도 권역 분할에는 중요한 근거입니다.       // 단, 실제 지도에는 별도 원/마커를 그리지 않습니다.       if(base && !entry.isRoad){         zoneEvidencePoints.push({lat:+base.lat,lng:+base.lng,name:base.name||entry.term,address:base.address||'',term:entry.term,district:base.district||routePointAnyDistrict(base)||''});       }       const anchors=await serviceAnchorsForEntry(entry,spec);       l
st zone={polygon,type,...meta,points:[...(exactRings[0]||[])],rings:normalizedDisplayRings,hitRings:exactRings};   kakao.maps.event.addListener(polygon,'mouseover',e=>{     polygon.setOptions({fillOpacity:meta.manual?.38:(meta.ambiguous?.26:(contract?.48:(rural?.40:.34)))});     const ratioText=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';     const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);     const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));     showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);   });   kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));   kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return} 
e={polygon,type,...meta,points:[...(exactRings[0]||[])],rings:normalizedDisplayRings,hitRings:exactRings};   kakao.maps.event.addListener(polygon,'mouseover',e=>{     polygon.setOptions({fillOpacity:meta.manual?.38:(meta.ambiguous?.26:(contract?.48:(rural?.40:.34)))});     const ratioText=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';     const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);     const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));     showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);   });   kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));   kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return}     up
tOptions({fillOpacity:meta.manual?.38:(meta.ambiguous?.26:(contract?.48:(rural?.40:.34)))});     const ratioText=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';     const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);     const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));     showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);   });   kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));   kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return}     updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'
=meta.estimated&&Number.isFinite(+meta.neighborRatio)&&+meta.neighborRatio>0?` · 인접권역 ${Math.round(+meta.neighborRatio*100)}% 우세`:'';     const detail=meta.estimated?`${serviceZoneLabel(zone)} · 확정 수거노선 밖 추정권역 (추정)${ratioText}`:(meta.ambiguous?'자동 권역 경계부 · 담당자 확인 권장':`${serviceZoneLabel(zone)}${contract?' · 제일환경 대행업체 구간':(meta.riGrouped?' · 리 단위 통합권역':(meta.manual?' · 관리자 수동권역':' · 자동 추정권역'))}`);     const basis=meta.manual?'관리자 직접 지정':(meta.estimated?'확정 표시 수거노선 20m 범위를 제외한 빈 구간을 주변 원본 노선 기준으로 추정':(contract?'오창산업단지 대행업체 구간 · 리 단위 표시':(meta.riGrouped?'읍·면 법정리 1개 = 수거권역 1개':'원본 수거노선 최근접 분할')));     showZoneTooltip(e.latLng,`${meta.district||''}${meta.riName?' '+meta.riName:''}`,detail,basis);   });   kakao.maps.event.addListener(polygon,'mousemove',e=>moveZoneTooltip(e.latLng));   kakao.maps.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return}     updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);     if(activeLayer===type){ 
s.event.addListener(polygon,'mouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return}     updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);     if(activeLayer===type){       renderServiceZoneSchedule(type,{vehicle:zone.vehicle,vehicles:zone.vehicles,provider:zone.provider,plate:zone.plate,days:zone.days,riGrouped:!!zone.riGrouped,contractLayer:!!zone.contractLayer,zoneStatus:meta.manual?'manual':(meta.estimated?'estimated':(meta.ambiguous?'boundary':'inferred')),estimated:!!meta.estimated,neighborRatio:Number.isFinite(+meta.neighborRatio)?+meta.neighborRatio:0,neighborSupport:+meta.neighborSupport||0,neighborTotal:+meta.neighborTotal||0,distanceKm:Infinity},{district:meta.district||'',legalEmd:meta.district||'',legalRi:meta.riName||''},{fromMap:true});     }   });   serviceZoneOverlays[type].push(zone);return zone; } function featureOuterPathPoints(feature){   const rings=outerRingsOfFeature(feature);return rings.flatMap(r=>r.map(([lng,lat])=>({lat:+lat,
ouseout',()=>{polygon.setOptions({fillOpacity:baseFill});hideZoneTooltip()});   kakao.maps.event.addListener(polygon,'click',()=>{     if(meta.noSchedule){updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · 리 단위 구분 · 실제 수거일정은 표시 수거노선 20m 이내에서만 판정`);return}     updateMapBadge(`${meta.district||''}${meta.riName?' '+meta.riName:''} · ${serviceZoneLabel(zone)} · ${contract?'제일환경 대행업체 구간':(meta.riGrouped?'리 단위 수거권역':(meta.manual?'관리자 수동권역':'수거노선 근거 자동추정권역'))}${meta.estimated?' · (추정)':(meta.ambiguous?' · 경계부 확인 필요':'')}`);     if(activeLayer===type){       renderServiceZoneSchedule(type,{vehicle:zone.vehicle,vehicles:zone.vehicles,provider:zone.provider,plate:zone.plate,days:zone.days,riGrouped:!!zone.riGrouped,contractLayer:!!zone.contractLayer,zoneStatus:meta.manual?'manual':(meta.estimated?'estimated':(meta.ambiguous?'boundary':'inferred')),estimated:!!meta.estimated,neighborRatio:Number.isFinite(+meta.neighborRatio)?+meta.neighborRatio:0,neighborSupport:+meta.neighborSupport||0,neighborTotal:+meta.neighborTotal||0,distanceKm:Infinity},{district:meta.district||'',legalEmd:meta.district||'',legalRi:meta.riName||''},{fromMap:true});     }   });   serviceZoneOverlays[type].push(zone);return zone; } function featureOuterPathPoints(feature){   const rings=outerRingsOfFeature(feature);return rings.flatMap(r=>r.map(([lng,lat])=>({lat:+lat,lng:+lng}))); } function whole
rue;   }   if(RURAL_INFERRED_ROUTE_DISTRICTS.has(region.district)){     wholeFeatureZone(type,region.feature,{...meta,noSchedule:true,groupOutline:true,days:[]});     await buildRuralRouteBufferZones(type,region);     return true;   }   wholeFeatureZone(type,region.feature,meta);   return true; }  async function buildGridServiceZonesForRegion(type,region,groups){   const b=featureBounds(region.feature);if(!b||!groups.length)return;   const rural=RI_LAYER_DISTRICTS.has(region.district);   // 동지역 추정권역은 확정 표시노선 주변을 빼고 남은 공간만 채웁니다.   // 약 70~85m 격자로 샘플링해 확정노선이 지나가는 셀을 추정폴리곤에서 제외합니다.   const latStep=.00068,lngStep=.00086;   const ny=Math.max(1,Math.ceil((b.maxLat-b.minLat)/latStep));   const nx=Math.max(1,Math.ceil((b.maxLng-b.minLng)/lngStep));   const grid=Array.from({length:ny},()=>Array(nx).fill(null));   const groupByKey=new Map(groups.map(g=>[g.key,g]));    function touchesConfirmedRoute(lat0,lat1,lng0,lng1){     const lat=(lat0+lat1)/2,lng=(lng0+lng1)/2;     const samples=[       [lat,lng],[lat0,lng0],[lat0,lng1],[lat1,lng0],[lat1,lng1],       [lat0,lng],[lat1,lng],[lat,lng0],[lat,lng1]     ];     for(const [y,x] of samples){       for(const g of groups){         if(serviceGroupRouteDistanceKm(g,y,x)<=MAX_ADDRESS_ROUTE_DISTANCE_KM)return true;       }     }     return false;   }    // 1) 확정 수거선 20m 범위를 제외한 빈 공간만 추정 셀로 만듭니다.   for(let iy=0;iy<ny;iy++){     const lat0=b.minLat+iy
 wholeFeatureZone(type,region.feature,{...meta,noSchedule:true,groupOutline:true,days:[]});     await buildRuralRouteBufferZones(type,region);     return true;   }   wholeFeatureZone(type,region.feature,meta);   return true; }  async function buildGridServiceZonesForRegion(type,region,groups){   const b=featureBounds(region.feature);if(!b||!groups.length)return;   const rural=RI_LAYER_DISTRICTS.has(region.district);   // 동지역 추정권역은 확정 표시노선 주변을 빼고 남은 공간만 채웁니다.   // 약 70~85m 격자로 샘플링해 확정노선이 지나가는 셀을 추정폴리곤에서 제외합니다.   const latStep=.00068,lngStep=.00086;   const ny=Math.max(1,Math.ceil((b.maxLat-b.minLat)/latStep));   const nx=Math.max(1,Math.ceil((b.maxLng-b.minLng)/lngStep));   const grid=Array.from({length:ny},()=>Array(nx).fill(null));   const groupByKey=new Map(groups.map(g=>[g.key,g]));    function touchesConfirmedRoute(lat0,lat1,lng0,lng1){     const lat=(lat0+lat1)/2,lng=(lng0+lng1)/2;     const samples=[       [lat,lng],[lat0,lng0],[lat0,lng1],[lat1,lng0],[lat1,lng1],       [lat0,lng],[lat1,lng],[lat,lng0],[lat,lng1]     ];     for(const [y,x] of samples){       for(const g of groups){         if(serviceGroupRouteDistanceKm(g,y,x)<=MAX_ADDRESS_ROUTE_DISTANCE_KM)return true;       }     }     return false;   }    // 1) 확정 수거선 20m 범위를 제외한 빈 공간만 추정 셀로 만듭니다.   for(let iy=0;iy<ny;iy++){     const lat0=b.minLat+iy*latStep,lat1=Math.min(b.maxLat,lat0+latStep),lat=(lat0+lat1)/2;     fo
 Map(groups.map(g=>[g.key,g]));    function touchesConfirmedRoute(lat0,lat1,lng0,lng1){     const lat=(lat0+lat1)/2,lng=(lng0+lng1)/2;     const samples=[       [lat,lng],[lat0,lng0],[lat0,lng1],[lat1,lng0],[lat1,lng1],       [lat0,lng],[lat1,lng],[lat,lng0],[lat,lng1]     ];     for(const [y,x] of samples){       for(const g of groups){         if(serviceGroupRouteDistanceKm(g,y,x)<=MAX_ADDRESS_ROUTE_DISTANCE_KM)return true;       }     }     return false;   }    // 1) 확정 수거선 20m 범위를 제외한 빈 공간만 추정 셀로 만듭니다.   for(let iy=0;iy<ny;iy++){     const lat0=b.minLat+iy*latStep,lat1=Math.min(b.maxLat,lat0+latStep),lat=(lat0+lat1)/2;     for(let ix=0;ix<nx;ix++){       const lng0=b.minLng+ix*lngStep,lng1=Math.min(b.maxLng,lng0+lngStep),lng=(lng0+lng1)/2;       if(!pointInGeoFeature([lng,lat],region.feature))continue;       const cls=classifyServiceGroups(groups,lat,lng);if(!cls)continue;       if(touchesConfirmedRoute(lat0,lat1,lng0,lng1))continue;       grid[iy][ix]={ix,iy,key:cls.group.key,ambiguous:cls.ambiguous,estimated:true,group:cls.group,lng0,lng1,lat0,lat1};     }     if(iy%8===0)await sleepMs(0);   }    function dominantNeighborForBoundary(iy,ix,cell){     for(const radius of [1,2,3]){       const counts=new Map();let total=0;       for(let dy=-radius;dy<=radius;dy++)for(let dx=-radius;dx<=radius;dx++){         if(!dx&&!dy)continue;         if(Math.max(Math.abs(dx),Math.abs(dy))!=
x<=3;dx++){       if(!dx&&!dy)continue;       const n=grid[iy+dy]?.[ix+dx];if(!n)continue;       counts.set(n.key,(counts.get(n.key)||0)+1);total++;     }     if(counts.size){       const ranked=[...counts.entries()].sort((a,b)=>b[1]-a[1]||(a[0]===cell.key?-1:(b[0]===cell.key?1:String(a[0]).localeCompare(String(b[0]),'ko'))));       const top=ranked[0];return {key:top[0],support:top[1],total,ratio:top[1]/Math.max(1,total)};     }     return {key:cell.key,support:0,total:0,ratio:0};   }    // 2) 추정권역 경계셀은 주변 노선 점유비율이 높은 쪽에 편입하되 계속 (추정)으로 유지합니다.   for(let iy=0;iy<ny;iy++)for(let ix=0;ix<nx;ix++){     const cell=grid[iy][ix];if(!cell?.ambiguous)continue;     const choice=dominantNeighborForBoundary(iy,ix,cell);     const chosen=groupByKey.get(choice.key)||cell.group;     cell.key=chosen.key;cell.group=chosen;cell.ambiguous=false;cell.estimated=true;     cell.neighborRatio=choice.ratio;cell.neighborSupport=choice.support;cell.neighborTotal=choice.total;   }    // 3) 같은 추정 일정끼리 큰 블록으로 병합합니다. 배포 내장본에서는 이 블록들을 다시 실제 폴리곤으로 합칩니다.   const finished=[];let active=new Map();   for(let iy=0;iy<ny;iy++){     const runs=mergedRowRuns(grid[iy].filter(Boolean));     const next=new Map();     for(const run of runs){       const id=`${run.sig}|${run.x0}|${run.x1}`;       const prev=active.get(id);       if(prev&&prev.iy1===iy-1){         prev.iy1=iy;prev.lat1=run.lat1;         prev.neighborSupport+=
e;       const n=grid[iy+dy]?.[ix+dx];if(!n)continue;       counts.set(n.key,(counts.get(n.key)||0)+1);total++;     }     if(counts.size){       const ranked=[...counts.entries()].sort((a,b)=>b[1]-a[1]||(a[0]===cell.key?-1:(b[0]===cell.key?1:String(a[0]).localeCompare(String(b[0]),'ko'))));       const top=ranked[0];return {key:top[0],support:top[1],total,ratio:top[1]/Math.max(1,total)};     }     return {key:cell.key,support:0,total:0,ratio:0};   }    // 2) 추정권역 경계셀은 주변 노선 점유비율이 높은 쪽에 편입하되 계속 (추정)으로 유지합니다.   for(let iy=0;iy<ny;iy++)for(let ix=0;ix<nx;ix++){     const cell=grid[iy][ix];if(!cell?.ambiguous)continue;     const choice=dominantNeighborForBoundary(iy,ix,cell);     const chosen=groupByKey.get(choice.key)||cell.group;     cell.key=chosen.key;cell.group=chosen;cell.ambiguous=false;cell.estimated=true;     cell.neighborRatio=choice.ratio;cell.neighborSupport=choice.support;cell.neighborTotal=choice.total;   }    // 3) 같은 추정 일정끼리 큰 블록으로 병합합니다. 배포 내장본에서는 이 블록들을 다시 실제 폴리곤으로 합칩니다.   const finished=[];let active=new Map();   for(let iy=0;iy<ny;iy++){     const runs=mergedRowRuns(grid[iy].filter(Boolean));     const next=new Map();     for(const run of runs){       const id=`${run.sig}|${run.x0}|${run.x1}`;       const prev=active.get(id);       if(prev&&prev.iy1===iy-1){         prev.iy1=iy;prev.lat1=run.lat1;         prev.neighborSupport+=(+run.neighborSupport||0);         pr
